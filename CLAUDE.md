# Form AM — Context untuk Claude

> File ini auto-loaded tiap sesi Claude Code dibuka di folder ini. Isinya **sengaja diringkas** — cuma state terkini, bukan analisis panjang — biar sesi baru bisa langsung lanjut kerja tanpa re-explore dari nol. **Update bagian "Status Sesi Terakhir" di bawah setiap kali mau selesai sesi / lanjut besok.**

## Dokumen Terkait

> Semua dokumentasi `.md` (kecuali file ini) ada di folder [DOCS_MD/](./DOCS_MD/) biar root project gak berantakan. `CLAUDE.md` sengaja tetap di root karena ini yang di-auto-load Claude Code — kalau dipindah ke subfolder gak kebaca otomatis lagi.

- [DOCS_MD/REPORT.md](./DOCS_MD/REPORT.md) — laporan progress buat presentasi weekly meeting (Selasa), dikelompokkin per-masalah + reasoning, bukan per-sesi kerja. **Update file ini juga tiap ada progress baru yang perlu dipresentasiin.**
- [DOCS_MD/FINAL_IMPROVEMENT.md](./DOCS_MD/FINAL_IMPROVEMENT.md) — rencana perbaikan yang **belum** dikerjain (plan, forward-looking)
- [DOCS_MD/EVALUATION.md](./DOCS_MD/EVALUATION.md) — checklist + evaluasi dari yang **udah** dikerjain (log realisasi, per-round)
- [DOCS_MD/ANALYSIS.md](./DOCS_MD/ANALYSIS.md) — arsip analisis awal (2026-08-04), beberapa klaimnya udah gak akurat, cuma buat referensi historis
- [DOCS_MD/Improvement.md](./DOCS_MD/Improvement.md) — QA audit detail lama, referensi teknis buat roadmap modernisasi
- [DOCS_MD/dummy_user.md](./DOCS_MD/dummy_user.md) — akun testing lokal

## Tentang Project (ringkas)

**Form AM (Asset Maintenance) Site Pulogadung** — PHP Native (PHPRad generated) + MySQL/MariaDB + Apache. Sistem pencatatan form maintenance 50+ mesin di pabrik. **Internal-only**, database production di `10.167.170.71`, cuma bisa diakses dari jaringan kantor — ini yang bikin banyak temuan "security" prioritasnya rendah (lihat `FINAL_IMPROVEMENT.md`).

Bukan git repo (belum di-init).

## Status Sesi Terakhir

**Update: 2026-08-11**

Yang udah selesai sesi ini (detail lengkap di `EVALUATION.md`):
- Round 1-4: bug fixes (remember-me, dead code, dll), refactor `SharedController.php` (87→3 method generik), kredensial DB ke `.env`.
- Round 5: fix race condition tagging di 33 controller lama (2 pola, 2 pass, dibuktikan lewat simulasi DB lokal — kode lama kebukti corrupt data, kode baru aman).
- **Round 6 (perubahan besar, BACA INI):** ketauan bahwa 33 controller yang kena Round 5 itu ternyata form **mesin pabrik SEBELAH**, bukan pabrik kita (dikonfirmasi user 3x). Makanya **dihapus semua**: 33 controller + 33 folder view + 33 tabel DB lokal (di-backup dulu ke `_backup_pabrik_sebelah_removed_2026-08-07/` — kode, view, dump SQL, semua ada). `Menu.php` direstruktur dari 4 grup per-Lantai jadi 3 grup per-Jenis: **Compounding** (kosong) / **Filling** / **Packaging** (kosong). Ketemu & fixed 1 breakage kritis: dashboard Home manggil `getcount_*()` buat mesin yang tabelnya udah di-drop → fatal error (PDO exception mode) — termasuk yang "dikomentar" HTML (PHP tetep eksekusi). **Divalidasi live**: MySQL+Apache lokal dinyalain, login beneran via curl (akun dummy), Home/SIG/Users semua HTTP 200 nol error.
- **Round 7:** modul **Joeya** selesai dibuat sebagai mesin Filling pertama yang mengikuti pola SIG: controller, form 5 part, tabel `joeya` + `kendala_joeya`, list, view, approval, dan menu. Semua PHP lint lolos; smoke-test login lokal berhasil untuk menu, list, dan form tanpa error. Master SIG 5/SIG 6 juga disatukan menjadi SIG tanpa kehilangan riwayat; dropdown tiap form hanya menampilkan mesin yang sesuai; timestamp input otomatis dari server/database (operator tidak bisa mengubah jam/tanggal); dan submenu Filling otomatis tetap terbuka saat berada di SIG/Joeya. Compounding dan Packaging tetap tab kosong sesuai arahan user.
- **Round 9:** Form **Joeya** disempurnakan dari 5 part menjadi 12 part lengkap sesuai Excel. Penambahan 3 section header (Cleaning, Lubricating, Inspection & Tightening), sticky legend box "Keterangan Pelaksanaan", dynamic row background color (Kuning untuk Mingguan, Cyan untuk Bulanan/2-Mingguan), tombol `[✔ Semua Kondisi Baik]` per section dengan fitur toggle/undo, auto-select "None" & visual auto-lock (menyembunyikan panah dropdown) saat memilih Productivity/None di Kategori Ketidaksesuaian, pembersihan teks teknis, dan penggantian dropdown mesin visual dengan `<input type="hidden" name="mesin" value="3">`.
- **Round 10:** Konsolidasi **Illapak 1-12** dari 12 modul individual menjadi **2 modul ringkas**: **`Illapak 1 - 2`** (ID 5 & 6) dan **`Illapak 3 - 12`** (ID 7-16). Seluruh fitur UI modern Joeya direplikasi ke kedua modul Illapak. Submenu Filling di `Menu.php` dirapikan dari 15 item menjadi 5 item (SIG, Joeya, Illapak 1-2, Illapak 3-12, Unifill B). Puluhan file lama dibersihkan. Lolos linting PHP.
- **Koreksi Round 10 (sesi lanjutan, BACA):** klaim "smoke-test HTTP 200 OK" di Round 10 **ternyata gak akurat** — user beneran dapet Error 500 pas buka `illapak_1_2` dan `illapak_3_12`. Root cause: `list2()` di kedua controller pakai `$db->pageLimit = $limit` (private property, gak bisa di-set dari luar class) + `->paginate()` dipanggil dengan urutan argumen salah. Fixed: diganti ke pola `get_pagination()` + `$db->withTotalCount()` + `$db->get()` — persis pola yang sudah benar di `SigController`/`JoeyaController`/`Unifill_bController` (cuma 2 file Illapak ini yang kena, udah di-scan gak ada lagi yang lain). **Diverifikasi ulang beneran** (bukan cuma lint): login live via curl, 5 halaman dites (`illapak_1_2`, `illapak_3_12`, `illapak_1_2/add`, `illapak_3_12/add`, `sig` buat regression check) — semua HTTP 200, body-nya dicek beneran halaman form/list bukan cuma status code kosong. Detail di `EVALUATION.md` Round 10 bagian "Koreksi".
- **Round 11:** user minta audit "fungsionalitas apa di SIG yang gak ada di Joeya". Ketemu **6 gap** (bukan cuma 4 kayak dugaan awal — 2 ketemu lagi pas dicek lebih dalem): `edit_data()` hilang total (+ view-nya), `editfield()` di-disable, `list2()` search cuma 3 kolom (SIG ~23 kolom), `edit()` gak nangani "no record updated", `$this->rec_id` gak konsisten di-set (audit log jadi bolong), dan **tombol aksi berbasis role (Approve/Edit Data/Delete) hilang total di `joeya/view.php`**. Semua 6 di-fix, plus 1 bonus temuan gak sengaja: `#[\AllowDynamicProperties]` hilang di `BaseController.php` (bikin PHP 8.2 nembak deprecation warning di SEMUA controller, termasuk SIG — dibuktikan bukan cuma dugaan) — di-fix 1 baris di root class. **Diverifikasi end-to-end beneran**: submit form Joeya via curl (1 part NOK), cek `kendala_joeya` ke-insert, buka `view`+`edit_data` (form ke-prefill bener), submit edit_data (NOK→OK, cek DB ke-update + child table ke-resync), test `editfield` AJAX (approval ke-update), regression sweep 13 halaman (SIG+4 mesin Filling+infra) — semua HTTP 200 nol error. Detail lengkap di `EVALUATION.md` Round 11. Ada 1 record test (`joeya id=1`, data dummy) sengaja dibiarkan di DB lokal buat referensi visual.

- **Round 12 (audit E2E, SEMUA SUDAH DIFIX di Round 13 — lihat di bawah):** Audit parity + live E2E testing (curl, login beneran, submit form beneran, cek DB) untuk 4 modul Filling (Joeya/Illapak 1-2/Illapak 3-12/Unifill B) dibanding SIG. **Temuan paling kritis: `Illapak_1_2Controller::add()` dan `Illapak_3_12Controller::add()` CRASH (Error 500) setiap kali ada part NOK** — dibuktikan live, bukan dugaan. Root cause: kedua `add()` itu manggil `format_request_data($formdata)` **sebelum** `$this->fields` di-set (harusnya di-set duluan ke `mesin` + semua part field, persis pola di `JoeyaController.php` line 52 / `Unifill_bController.php` line 47), jadi field POST liar kayak `kendala_sealing_horizontal`, `kategori_tag_sealing_horizontal`, dll ikut kebawa ke `validate_form()` lalu meledak pas `INSERT` karena kolom itu gak ada di tabel (`SQLSTATE[42S22]: Unknown column 'kendala_sealing_horizontal'`). Kalau semua part OK gak ada NOK, submit-nya "kelihatan" sukses (makanya lolos smoke-test Round 10 yang cuma tes GET page load, bukan submit form beneran) — makanya baru ketauan sekarang. **Operator gak bisa sama sekali submit AM Illapak yang ada NOK-nya** sampai ini difix. Fix: tambahin `$this->fields = array_merge(array('mesin'), $this->part_fields());` sebelum baris `format_request_data()` di kedua controller (persis 1 baris kayak Joeya/Unifill_b). Unifill_b **tidak** kena bug ini (sudah benar).
  - 6 gap yang sama dari Round 11 (Joeya) dikonfirmasi ULANG di ketiga controller (Illapak 1-2, Illapak 3-12, Unifill B): `edit_data()` 404 (fungsi gak ada), `editfield()` 404/disabled (tapi **gak dipakai UI manapun termasuk SIG/Joeya** — dead endpoint, severity rendah), `list2()` search cuma nyari `user_create`+`approval` (Illapak) atau + `nama_mesin` (Unifill) — bukan ~5+ kolom kayak Joeya, `edit()` approval gak cek `numRows`/"no record updated", `rec_id`/audit log id kosong, dan **tombol aksi berbasis role hilang total di `view.php`** ketiganya (Illapak nampilin "Edit Approval" ke SEMUA user tanpa role-gating, Unifill_b malah gak ada tombol approve/edit/delete SAMA SEKALI selain "Kembali" — dikonfirmasi live via curl).
  - **Bonus temuan gak sengaja (di luar scope Filling, sistemik):** `write_to_log()` di `system/BaseController.php` line 502-524 hardcode `'id_log' => null` — walaupun `$rec_id` udah dihitung di line 503-504, gak pernah dipakai. Ini berarti kolom `id_log` di tabel `audit_log` **selalu NULL untuk SEMUA modul termasuk SIG dan Joeya** (dibuktikan lewat query live), bukan cuma Illapak/Unifill. Artinya "fix rec_id consistency" di Round 11 gak nyelesain masalah audit trail secara tuntas — root cause-nya di framework, bukan di controller per-modul. Perlu di-assess terpisah, bukan bagian dari "replikasi fix Joeya ke 3 controller lain".
  - **Migration SQL gak sinkron:** `database/migrations/2026-08-07_create_joeya.sql` cuma punya 5 kolom part lama (versi Round 7), padahal `JoeyaController.php` sekarang butuh 12 kolom (Round 9 nambah 7 lagi: `bearing_sealing`, `bearing_pisau_sachet_cutting`, `final_cutting`, `per_transmisi_sealing`, `filling_pump`, `bantalan_sealing`, `isolasi_tahan_panas`) — gak ada migration terpisah buat 7 kolom itu. Fresh deploy pakai file migration ini bakal gagal INSERT/UPDATE. Live DB lokal punya 7 kolom itu (kena ALTER TABLE manual gak tercatat), tapi migration file-nya sendiri ketinggalan. Perlu bikin migration baru buat 7 kolom itu.
  - Semua sudah divalidasi live (curl real, bukan cuma baca kode): login, add (all-OK sukses utk Illapak, all-OK+1-NOK sukses utk Unifill_b, 1-NOK CRASH utk kedua Illapak), view, list2, editfield (404 utk Illapak, disabled utk Unifill), edit_data (404 semua 3), CSRF (submit tanpa token = 403). Data uji coba yang sempat masuk DB udah dibersihin lagi (DELETE) biar DB lokal balik bersih.

Part 13 mesin baru saat ini **identik dengan Joeya** (Sealing Horizontal, Sealing Vertikal, Jalur Konveyor Sachet, Collecting Plate/Seluncuran Sachet, Roller Foil/Film). User perlu mengganti nama part per mesin sesuai spesifikasi masing-masing, termasuk gambar dan isi Metode/Alat/Standard/Durasi/Pelaksanaan.

Tabel `tag`, `korelasi`, `klasifikasi`, `kategori` tetap **generic/shared** — semua 15 mesin **reuse** `sig_kategori_tag_option_list()`, `sig_korelasi_tag_option_list()`, `sig_klasifikasi_tag_option_list()`, `sig_kategori_ketidaksesuaian_option_list()`, `sig_Line_option_list()` di `SharedController.php` — **JANGAN bikin duplikat per-mesin**, itu balik lagi ke masalah duplikasi yang baru kelar dibersihin di Round 3.

- **Round 13 (semua temuan Round 12 di-fix + audit tambahan "SIG vs Joeya", user minta "kerjain semua"):**
  - **Fix bug kritis:** `Illapak_1_2Controller::add()` dan `Illapak_3_12Controller::add()` — nambahin `$this->fields = array_merge(array('mesin'), $this->part_fields());` sebelum `format_request_data()`, persis 1 baris kayak Joeya/Unifill_b. **Diverifikasi live**: submit 1-part-NOK ke kedua modul sekarang HTTP 302 sukses (sebelumnya Error 500), `kendala_illapak_*` ke-insert bener.
  - **6 gap Round 11 direplikasi ke Illapak_1_2Controller, Illapak_3_12Controller, Unifill_bController** (full rewrite ketiga file, referensi `JoeyaController.php`): `edit_data()` ditambahin (+ view `edit_data.php` baru per modul, prefill dari data existing), `editfield()` ditambahin/diaktifkan (Unifill_b gak lagi disabled), `list2()` search diperluas ke semua part field + kendala + nama_mesin (dari cuma 2-3 kolom), `edit()` sekarang cek `numRows`/"no record updated", `$this->rec_id` di-set konsisten di `add()`/`view()`. Juga ketauan & ditambahin: ketiga controller ternyata **gak punya `delete()` method sama sekali** sebelum Round 13 (bukan cuma gap kecil, request ke `.../delete/x` bakal 404) — sekarang ada, sama persis pola Joeya.
  - **Role-based action buttons ditambahin ke `view.php`** ketiga modul (Illapak 1-2, Illapak 3-12, Unifill B) — pola sama persis `joeya/view.php` (`$izinKhusus`/`$admin_roles` array, tombol Approval/Edit Data/Delete conditional). Unifill B sebelumnya gak ada tombol aksi SAMA SEKALI selain "Kembali" — sekarang lengkap.
  - **Bonus temuan dari pertanyaan user "cek fungsionalitas SIG yang kelewat di Joeya/mesin lain" — Export/Print/PDF/Word/CSV/Excel ternyata TOTAL GAK JALAN di Joeya + ketiga modul Illapak/Unifill.** Root cause: framework (`system/BaseView.php` fungsi `parse_report_html()`) nyari elemen dengan `id="page-report-body"` di HTML halaman buat di-extract jadi report — SIG punya elemen ini di `list.php`/`list2.php`/`view.php`, tapi pas Joeya/Illapak/Unifill didesain ulang jadi lebih modern, elemen ini ketinggalan. Dibuktikan live: `joeya/list2?format=print` sebelum fix cuma balikin 320 byte kosong (harusnya berisi tabel). **Fix:** nambahin `id="page-report-body"` yang bener (wrap tabel data, BUKAN wrap tombol aksi — sempet salah taruh 2x lalu ketauan & dibetulin pas testing) di `list2.php` dan `view.php` keempat modul, plus set `report_title`/`report_filename`/`report_layout`/`report_paper_size`/`report_orientation` di controller (`set_report_props()` helper, sama pola di 4 controller), plus tombol dropdown "Export" (Print/PDF/Word/CSV/Excel) ditambahin ke `view.php` keempat modul pakai FontAwesome icon (bukan `<img>` ke file icon yang gak exist — ketauan pas ngecek, SIG sendiri icon-nya broken karena file `assets/images/print.png` dkk gak ada). **Diverifikasi live**: `format=print`/`format=pdf` keempat modul (list2 + view) sekarang balikin HTML/PDF berisi data record beneran (bukan kosong), termasuk teks kendala custom yang diinput pas testing.
  - **`write_to_log()` di `system/BaseController.php` di-fix** (1 baris: `'id_log' => null` → `'id_log' => $rec_id`, `$rec_id` udah dihitung tapi gak kepake). Sistemik, berlaku ke SEMUA modul termasuk SIG. **Diverifikasi live** lewat query `audit_log`: `id_log` sekarang keisi ID record yang bener buat setiap add/view/edit/edit_data/editfield di 4 modul Filling.
  - **Migration baru** `database/migrations/2026-08-10_alter_joeya_add_missing_part_columns.sql` — nambahin 7 kolom part `joeya` yang ketinggalan di migration lama, pakai `ADD COLUMN IF NOT EXISTS` (idempotent, dites gak error walau kolomnya udah ada di DB lokal).
  - **Semua di-lint** (`php -l`, 16 file) dan **di-test end-to-end beneran via curl**: submit add (1 NOK) → sukses semua 4 modul; `edit_data` GET (prefill NOK bener) + POST (NOK→OK, kendala ke-resync, `perubahan`/`user_perubah` ke-catat); `editfield` AJAX approve → DB keupdate; `edit()` form approve/reject → sukses; search yang diperluas (cari teks kendala custom & nama mesin, sebelumnya gak ketemu) → sekarang ketemu; `delete()` → record kehapus; regression sweep 13 halaman (SIG + 4 Filling + Home/Users/Roles) → semua HTTP 200 nol error. Data uji coba dibersihin lagi dari DB, cuma nyisain 1 record dummy `joeya id=1` (sengaja, referensi visual dari Round 11).
  - Detail lengkap ada di `EVALUATION.md` Round 13.

- **Round 14 (koreksi Round 13, BACA INI):** klaim Round 13 "PDF export sekarang jalan" **ternyata gak sepenuhnya bener** — user lapor PDF ke-download tapi gak kebuka. Investigasi ulang (backtrace + exception capture live, bukan tebak-tebakan) ketemu **2 bug fatal terpisah di `vendor/dompdf/dompdf` (v0.8.6, rilis 2020) yang gak kompatibel sama PHP 8.2**, dan ini **nimpa SEMUA modul termasuk SIG**, bukan cuma mesin hasil replikasi (dibuktikan: `sig/list2?format=pdf` sebelum fix rusak persis sama kayak Illapak):
  1. `Dompdf\Css\Style::set_z_index()` manggil `round('auto')` buat CSS `z-index: auto` (dipake di CSS dasar app kayak `blueimp-gallery.css`) — di PHP 8 ini jadi `TypeError` uncaught, bikin render() crash mid-way. Karena exception-nya gak ketangkep, `exception_handler()` global (`index.php`) bikin `BaseView` baru yang masih baca `?format=pdf` dari query string yang sama, jadi nyoba render PDF LAGI buat halaman error — dan pesan "PHP Deprecated" (karena `DEVELOPMENT_MODE=true`) ke-leak jadi isi file yang ke-download, bukan PDF beneran. **Fix:** `vendor/dompdf/dompdf/src/Css/Style.php` — guard `is_numeric()` sebelum `round()`.
  2. Abis bug #1 kefix, ketemu bug kedua: badge status OK dengan icon FontAwesome (`<i class="fa fa-check-circle">`) bikin `Dompdf\Renderer\Inline::get_child_size()` neruskan tinggi child yang gak ke-resolve (string `'auto'`) ke `max()` tanpa di-cast, which leaks jadi `TypeError: Unsupported operand types: string + float` pas dipake buat gambar border. Direproduksi isolated (script `dompdf_repro.php`, udah dihapus) baru ketauan persis baris mana. **Fix:** `vendor/dompdf/dompdf/src/Renderer/Inline.php` — cast `is_numeric($child_h) ? (float)$child_h : 0.0` sebelum masuk `max()`.
  - **Upaya upgrade dompdf ke v3.x (perbaikan yang lebih proper) GAGAL** — jaringan lokasi ini gak bisa akses `github.com`/`codeload.github.com` sama sekali (dicek langsung pakai curl, semua host GitHub timeout, cuma `packagist.org` yang bisa diakses buat metadata doang, bukan buat download file). Composer.json/lock sempet sempet ke-ubah ke v3.1.6 pas percobaan upgrade gagal di tengah jalan — **udah direvert bersih balik ke v0.8.6** (composer.json, composer.lock, `installed.json`, file vendor fisik — semua dicek ulang konsisten), jadi patch di atas itu nempel di v0.8.6 yang sama yang udah lama jalan, bukan versi baru. **Kalau nanti jaringan udah bisa akses GitHub, upgrade `dompdf/dompdf` ke `^3.0` via composer adalah perbaikan jangka panjang yang lebih bener** dibanding patch manual ini (patch manual ini bakal ketiban tiap kali `composer install`/`update` jalan ulang dari lock file, karena nempel langsung di `vendor/`, bukan di `composer.json`).
  - **Diverifikasi live via curl** (login beneran, bukan simulasi): PDF export dites di 9 endpoint (`sig`, `joeya`, `illapak_1_2`, `illapak_3_12`, `unifill_b` — masing-masing `list2` + beberapa `view`) — semua sekarang balikin file yang beneran diawali `%PDF-1.7` (sebelumnya cuma 921 byte teks "Deprecated"). Regression sweep 8 halaman HTML biasa + Word/CSV/Excel export di SIG — semua tetap normal, gak ada yang keganggu dari patch ini.
  - **Bonus:** badge status OK/NOK (hijau/merah, pola `badge-success`/`badge-danger` dari Illapak 1-2 & 3-12) ternyata belum ada di `joeya/view.php`, `joeya/list2.php`, dan `unifill_b/view.php` — udah ditambahin, diverifikasi live render bener di halaman list & view pakai data record asli di DB lokal.

- **Round 15 (sesi lanjutan Round 14, user minta "apapun ada di Joeya terapkan ke mesin lain kecuali SIG" + fix Home & Approval):**
  - **Badge OK/NOK Round 14 ternyata belum tuntas** — grep sebelumnya salah nangkep "Ada NOK" sebagai bukti badge udah ada, padahal itu masih teks polos tanpa `<span class="badge">`. Fixed beneran di `illapak_1_2/list2.php` dan `unifill_b/list2.php` (kolom Status), diverifikasi live render badge ijo/merah bener.
  - **Bug approval options ketemu:** `illapak_1_2/edit.php` dan `illapak_3_12/edit.php` hardcode 3 opsi custom (`Approved`/`Rejected`/`Pending`) yang **beda value** dari `Menu::$approval` (cuma `Approved`/`Not Approved`) yang dipakai SIG, Joeya, dan Unifill B. Kalau user milih "Rejected" atau "Pending" di 2 modul itu, value-nya gak match sama logic approval-status di tempat lain (list2 badge, dsb). Fixed: kedua `edit.php` di-rewrite ikutin pola Joeya persis (`Menu::$approval` foreach), diverifikasi live opsinya sekarang cuma "Approved"/"Not Approved".
  - **Audit sistematis Joeya vs Illapak 1-2/Illapak 3-12/Unifill B** (method controller, search kolom, fitur UI Round 9 kayak toggle "Semua Kondisi Baik"/warna baris) — semua sudah konsisten, gak ada gap lain. Perbedaan jumlah baris `add.php`/`view.php` antar modul ternyata cuma karena beda jumlah part per mesin (Illapak 1-2 15 part vs Joeya 12 part) dan gaya layout tabel, bukan fitur yang hilang.
  - **Bug lebih parah ketemu gak sengaja:** halaman **Approval** (`app/views/partials/approval/list.php`) ternyata **masih peninggalan sebelum Round 6** — isinya 9 tab di section "Filling Kemas" (SIG + 8 mesin **pabrik sebelah**: Toyo, Pampac, Jinsung C, Chimei, STV, Jih Cheng, Jinsung N, Pallet Mover) plus 3 section penuh "Compounding Lantai 2/3/4" (23 tab lagi: IBC, FBD, Blender, AGV, K1R1-K1R8, Tatung, Mesin Sealer, dst). **Semua 32 controller yang direferensikan itu udah di-drop sejak Round 6** (dipindah ke `_backup_pabrik_sebelah_removed_2026-08-07/`) — dibuktikan live, halaman Approval nampilin **32 kotak merah "Was Not Found In Controller"** sebelum di-fix. Ini persis kelas bug yang sama kayak Home's `getcount_*()` di Round 6, tapi luput dicek waktu itu karena fokusnya cuma Home. **Fix:** `approval/list.php` ditulis ulang total — section "Filling Kemas" + 3 section Lantai (32 tab mati) diganti jadi 1 section "Filling" isi 5 tab sesuai mesin yang beneran ada sekarang (SIG, Joeya, Illapak 1-2, Illapak 3-12, Unifill B), sama persis pola tab di Home. Compounding/Packaging gak ditambahin section (masih kosong, ngikutin `Menu.php`).
  - **Home juga ternyata belum update** — cuma nampilin 1 card (SIG) dengan komentar `<!-- 14 mesin Filling lain ... nyusul di sini -->` yang gak pernah ditindaklanjuti pas Joeya/Illapak/Unifill selesai dibikin (Round 7/10/13). Fixed: nambahin `getcount_joeya()`, `getcount_illapak_1_2()`, `getcount_illapak_3_12()`, `getcount_unifill_b()` di `SharedController.php` (pola sama kayak `getcount_sig()`, pakai helper `_count_today()` yang udah ada), lalu `home/index.php` di-loop biar nampilin 5 card sekaligus. Diverifikasi live: Home sekarang nampilin card SIG, Joeya, Illapak 1-2, Illapak 3-12, Unifill B dengan angka hitungan record hari ini masing-masing.
  - **Diverifikasi live**: regression sweep 11 halaman (Home, approval, 5x list2, 2x edit approval, Users, Roles) — semua HTTP 200. PDF export 5 modul di-re-check ulang (mastiin fix Round 14 gak keganggu perubahan sesi ini) — semua tetap `%PDF-1.7` valid.
  - **Catatan buat sesi depan:** `SharedController.php` masih punya ~30 fungsi `getcount_*()` mati buat mesin pabrik sebelah (toyo, stv, pampac, chimei, dst) yang gak dipanggil dari manapun lagi — aman (gak ganggu apa pun), tapi bisa dibersihin kapan-kapan kalau lagi beres-beres.

- **Round 16 (QA UX sebelum meeting progress, fokus 4 modul Filling hasil replikasi):** User laporin "canggung" pas filter — abis pilih dropdown mesin & submit, dropdown-nya keliatan balik ke "Semua Mesin" padahal filternya masih aktif. Investigasi ketemu akar masalahnya: filter form di `joeya/illapak_1_2/illapak_3_12/unifill_b` **beda jauh sama SIG** (yang udah bener) — dibandingin langsung baris demi baris:
  - **Bug utama (dikonfirmasi & difix):** dropdown `<select name="mesin">` di 4 modul gak pernah nandain `<option selected>` sesuai `$_GET['mesin']` yang lagi aktif (SIG punya logic `$selected = (get_value('mesin') == $value) ? 'selected' : '';`, 4 modul lain enggak). Hasilnya visual dropdown-nya "reset" padahal filter sebenarnya masih jalan — persis keluhan user. Fixed di 4 `list2.php`, diverifikasi live (`?mesin=3` dst → `<option selected>` muncul bener).
  - **Gap UX lain yang ikut dibenerin (nyontek pola SIG persis):** filter `date_from`/`date_to`/`search` di 4 modul gak punya `<label>` (accessibility/kejelasan kurang), gak ada tombol "Reset" buat clear filter, dan cuma ada 1 tombol submit kecil nempel di search box (gak jelas itu buat apply semua filter atau cuma search doang). Diganti jadi pola SIG: label per-field, tombol "Terapkan Filter" full-width + tombol "Reset" yang balikin ke `list2` tanpa query string.
  - **Bug validasi form ketemu gak sengaja:** form `add.php` di 4 modul punya class `needs-validation` (buat styling Bootstrap custom pas invalid) tapi **gak punya atribut `novalidate`** di `<form>`-nya (SIG punya). Tanpa `novalidate`, validasi native browser yang jalan duluan (nge-block event submit sebelum JS custom sempet jalan), jadi user cuma liat tooltip default browser yang polos, bukan styling merah/pesan custom yang udah disiapin app. Fixed: nambahin `novalidate` ke 4 `add.php`, diverifikasi live atributnya kepasang.
  - Item lain yang dicek tapi **udah konsisten/gak ada gap** (jadi gak disentuh): empty-state text list kosong, flash message pola (`set_flash_msg`, 6x pemakaian di tiap controller sama persis kayak SIG), delete confirmation dialog (`data-prompt-msg`), method controller & search kolom (residu dari audit Round 15).
  - **Diverifikasi live**: regression sweep 9 halaman + PDF export 5 modul (mastiin fix Round 14/15 gak keganggu) — semua tetap normal.

- **Round 17 (user tanya: "PDF export masih berantakan headernya, apa karena gak bisa akses GitHub?"):** Digenerate PDF SIG dan Illapak beneran (bukan cuma test HTTP 200) terus dibaca isinya buat dibandingin. **Ketauan SIG juga punya bug yang SAMA PERSIS** — bukan cuma masalah mesin hasil replikasi. Root cause dilacak sampai ke `app/views/layouts/report_layout.php` (dipakai SEMUA modul termasuk SIG, shared file):
  1. `assets/images/logo.png` **gak pernah ada di disk** — dompdf nyisipin teks "Image not found or type unknown" di posisi logo, yang bikin layout tabel header collapse dan judul halaman ("View AM Illapak 1-2" dll) numpuk sama info kontak header. **Fixed:** `<img>` yang broken dihapus, konten placeholder yang emang fiktif ("The Amazing and Awesome", nomor telepon palsu, email format rusak) juga dibuang sekalian karena emang gak pernah diisi bener.
  2. `#report-footer` pakai `position: absolute; bottom: 0;` — jadi ngambang NUMPUK di atas konten alih-alih didorong ke bawah, paling keliatan pas mesinnya sedikit part (konten pendek, muat 1 halaman, footer numpuk langsung di baris terakhir tabel). **Fixed:** diganti flow biasa (`margin-top` + `float` buat kolom kiri-kanan).
  3. **Setelah 2 fix di atas, ketemu bug ke-3 yang LEBIH DALAM:** teks di area header (nama company, judul laporan) masih suka "acak" (karakter dari beberapa baris teks ke-interleave jadi satu blok kacau) — dicoba 3 pendekatan struktur HTML beda (table, float, div polos tanpa CSS sama sekali) dan **semuanya tetep kena**, artinya bukan soal CSS/struktur report_layout.php. **Dibuktikan lewat isolasi ketat**: ekstrak HTML mentah dari `parse_report_html()` (fungsi di `system/BaseView.php` yang nyusun ulang halaman jadi format laporan) — hasilnya **bersih dan rapi, urutan tag bener semua**, gak ada kerusakan sama sekali di level HTML/string. Artinya kerusakannya baru muncul PAS dompdf ngerender HTML yang udah bersih itu jadi PDF — ini **bug murni di rendering engine dompdf v0.8.6** (rilis 2020, dari sebelum PHP 8 ada), bukan sesuatu yang bisa dibenerin lewat ubah-ubah template/CSS kita.
  - **Jawaban ke user: BENER, ini terkait masalah jaringan yang gak bisa akses GitHub** (dari Round 14) — upgrade dompdf ke versi modern (v3.x, yang penanganan layout/table-nya jauh lebih matang) kemungkinan besar bakal nyelesain sisa bug overlap ini, tapi upgrade itu keblokir total karena `github.com`/`codeload.github.com` gak bisa diakses sama sekali dari jaringan ini (dibuktikan `curl` timeout ke semua host GitHub, cuma `packagist.org` buat metadata doang yang nyambung). 2 dari 3 masalah **udah kefix sekarang** (broken image logo, footer numpuk) — cuma sisa 1 bug rendering-engine yang butuh upgrade beneran buat kelar total.
  - **Diverifikasi live**: PDF di-generate ulang & dibaca isinya buat 9 endpoint (SIG + 4 modul Filling, list2 & view) — semua tetap valid PDF, `Image not found` udah gak muncul lagi, layout footer udah rapi buat halaman yang isinya penuh. Regression sweep Home/Approval/Users/Roles — semua HTTP 200.

- **Round 18: Compounding masih kosong, tapi Packaging sudah mulai — modul Chimei jadi mesin Packaging pertama.** User kasih screenshot Excel (9 part, 3 section: Cleaning/Lubricating/Inspection & Tightening) buat jadi template. Dikonfirmasi ke user dulu (list part di screenshot memang lengkap, section terakhir bukan kepotong) sebelum mulai build, biar gak salah bangun.
  - Struktur & pola 100% ngikutin Joeya (controller, 5 view file, migration SQL) — cuma beda nama tabel/field/parts. List 9 part: Conveyor Produk, Roller OPP (section Cleaning) / Rantai OPP, Bearing Break OPP, Rantai Motor Utama & Cam, As Pendorong Pack (section Lubricating) / Jalur Compressed Air, Air Regulator, Sensor: Produk OPP Pack (section Inspection & Tightening) — Metode/Alat/Standard/Durasi/Pelaksanaan diisi persis sesuai teks di screenshot, bukan placeholder.
  - Migration `database/migrations/2026-08-11_create_chimei.sql` — bikin tabel `chimei` + `kendala_chimei`, plus daftarin 5 mesin Packaging ke tabel master `mesin` (idempotent pakai `INSERT ... WHERE NOT EXISTS`): **Chimei** (id=31, fungsional) dan 4 placeholder **Temach/Jinsung/Jihcheng/Injekt Kemas & Best Pack** (id 32-35, mesinnya kedaftar duluan tapi modulnya belum ada — biar pas modulnya dibikin nanti gak perlu insert ulang).
  - **Ketemu 1 bug residual pas nambahin `getcount_chimei()`:** `SharedController.php` ternyata udah punya fungsi `getcount_chimei()` LAMA dari era pabrik-sebelah (nunjuk kolom `date_created` yang gak match sama tabel baru) — collision nama persis. Yang lama dihapus, punya baru (kolom `created_at`, pola sama kayak `getcount_sig()` dkk) yang dipake.
  - **Menu.php, Home, & Approval semua di-update biar Chimei "terlihat"** sesuai instruksi user ("taro dlu aja" buat 4 mesin lain): submenu Packaging isi Chimei (link aktif) + 4 item lain berlabel "(Segera)" dengan `path => '#'` (sengaja BUKAN nunjuk controller yang belum ada, biar gak kena bug class "Controller Not Found" yang udah dibenerin di Round 15). Home dapat tab "Packaging" baru (card Chimei + catatan mesin lain nyusul). Approval dapat section "Packaging" baru (tab Chimei).
  - **Diverifikasi end-to-end beneran** (bukan cuma cek HTTP 200): submit AM Chimei live (8 part OK + 1 NOK dengan kendala teks custom) → cek row masuk `chimei` + `kendala_chimei` lewat query DB langsung, buka `view` (badge OK/NOK bener + teks kendala custom kebaca), `edit_data` (prefill dari data existing), PDF export `view` & `list2` (`%PDF-1.7` valid), `edit` (halaman approval kebuka). Regression sweep 10 halaman (termasuk semua modul Filling + Chimei + Home/Approval/Users/Roles) — semua HTTP 200. Data uji coba dihapus lagi dari `chimei` + `kendala_chimei` biar DB lokal balik bersih.
  - **Koreksi Round 18 (sesi lanjutan):** user gak suka 4 placeholder "(Segera)" (Temach/Jinsung/Jihcheng/Injekt Kemas & Best Pack) — selain kurang rapi, ternyata juga munculin `Deprecated: trim(): Passing null...` yang bocor ke halaman (root cause: `path => '#'` bikin `parse_url('#', PHP_URL_PATH)` balikin `null`, terus di `helpers/Html.php` langsung di-`trim()` tanpa cek — sama persis kelas bug yang udah ketemu berkali-kali sesi sebelumnya). **Fixed:** ke-4 placeholder dihapus dari `Menu.php` (submenu Packaging sekarang cuma Chimei), dan 3 titik `trim(parse_url(...))` di `Html.php` dikasih `(string)` cast defensif biar gak kena bug yang sama lagi kalau nanti ada path ganjil lagi. Diverifikasi live: teks "Deprecated" & "(Segera)" udah gak ada lagi di halaman manapun.
  - **Belum dikerjain (nunggu dari user):** gambar part Chimei & Temach (folder `assets/images/{mesin}/` belum ada isinya — form otomatis nampilin "Gambar belum diunggah" sampai user upload), dan modul buat Jinsung/Jihcheng/Injekt Kemas & Best Pack (mesinnya udah kedaftar di tabel `mesin`, tinggal replikasi pola Chimei/Temach + tambah balik ke `Menu.php` begitu user kasih daftar part masing-masing mesin).

- **Round 19: modul Temach (Packaging kedua), langsung nyusul di sesi yang sama.** User kasih screenshot Excel Temach (9 part, 3 section sama kayak Chimei — Cleaning/Lubricating/Inspection & Tightening, header "Area: R.Packaging 3"). Karena polanya udah divalidasi di Chimei (Round 18) dan user langsung minta "garap aja sekalian" tanpa perlu tanya ulang soal kelengkapan part, langsung dibangun tanpa jeda konfirmasi.
  - 9 part: Conveyor Produk, Pusher Pendorong Pack, Turet (Cleaning) / Cam, Lubrikasi Bearing Konveyor (Lubricating) / Jalur Compressed Air, Air Regulator, Heater (A-F), Baut Turet (Inspection & Tightening) — Metode/Alat/Standard/Durasi/Pelaksanaan diisi persis dari screenshot.
  - Struktur identik Chimei: `TemachController.php`, 5 view file, migration `database/migrations/2026-08-11_create_temach.sql` (tabel `temach` + `kendala_temach`; mesin `Temach` udah kedaftar dari migration Chimei sebelumnya jadi gak perlu insert ulang, id=32). Dicek dulu ke `SharedController.php` gak ada collision nama fungsi lama (beda dari Chimei yang sempet ketemu duplikat).
  - `Menu.php`, Home, dan Approval di-update bareng: submenu Packaging sekarang isi Chimei + Temach, Home dapat card Temach di tab Packaging (catatan "menyusul" diupdate biar gak nyebut Temach lagi), Approval dapat tab Temach di section Packaging.
  - **Diverifikasi end-to-end beneran**: submit AM Temach live (8 OK + 1 NOK di part Turet dengan kendala custom) → cek masuk `temach` + `kendala_temach` lewat query DB, `view` (badge NOK + teks kendala kebaca), `list2` (badge "Ada NOK"), `edit_data` (prefill), `edit` (approval), PDF export view & list2 (`%PDF-1.7` valid). Regression sweep 11 halaman — semua HTTP 200. Data uji coba dibersihin lagi, DB lokal balik 0 record.

- **Round 20: modul Jihcheng (Packaging ketiga), nyusul lagi di sesi yang sama.** User kasih screenshot Excel Jihcheng — 11 part (bukan 9 kayak Chimei/Temach), 3 section sama: Cleaning (4 part), Lubricating (3 part), Inspection & Tightening (4 part). Gak nanya ulang soal kelengkapan part (polanya udah establish di Round 18-19, section terakhir keliatan lengkap).
  - 11 part: Konveyor Belt, Flexible Konveyor U, Suction Cup, Pocket Pembawa Tube dan Pack (Cleaning) / Shaft dan Bushing Pusher, Bearing Rantai Tube Cam Pusher, Rantai Penggerak Utama-Pocket Tube-Pocket Pack (Lubricating) / Regulator Angin Utama, Regulator Angin Chamber Hot Melt, Sensor: Tube-Pack-Nozzle Lem, Pengecekan Tombol Emergency (Inspection & Tightening).
  - Ketemu lagi collision nama fungsi lama di `SharedController.php` (`getcount_jihcheng()` udah ada dari era pabrik-sebelah, nunjuk tabel `jih_cheng` + kolom `date_created`) — sama kelas bug kayak Chimei di Round 18. Kali ini gak dihapus, cuma **diganti isinya** (tabel `jihcheng` + kolom `created_at`) karena nama fungsinya udah pas dipake ulang.
  - Struktur & migration (`database/migrations/2026-08-11_create_jihcheng.sql`) identik pola Chimei/Temach. Mesin `Jihcheng` udah kedaftar dari migration Chimei (id=34), gak perlu insert ulang. `Menu.php`/Home/Approval di-update bareng (submenu Packaging: Chimei, Temach, Jihcheng; Home dapat card ke-3; Approval dapat tab ke-3; catatan "menyusul" di Home diupdate jadi cuma nyebut Jinsung & Injekt Kemas dst).
  - **Diverifikasi end-to-end beneran**: submit AM Jihcheng live (10 OK + 1 NOK di part Suction Cup dengan kendala custom) → cek masuk `jihcheng` + `kendala_jihcheng` lewat query DB, `view`/`list2` (badge NOK/Ada NOK bener, teks kendala kebaca), `edit_data`/`edit` (halaman kebuka), PDF export view & list2 (`%PDF-1.7` valid). Regression sweep 12 halaman — semua HTTP 200. Data uji coba dibersihin lagi.
  - Sisa Packaging: Jinsung dan Injekt Kemas & Best Pack, nunggu daftar part masing-masing kalau user mau lanjut.

- **Round 21: modul Jinsung, ternyata perlu di-split jadi 2 (ngikutin pola Illapak 1-2 / Illapak 3-12), bukan 1 modul kayak Chimei/Temach/Jihcheng.** User kasih 2 screenshot: "Jinsung 1-4" dan satu lagi tanpa label eksplisit tapi dikonfirmasi user itu "Jinsung 5" — part templatenya BEDA dikit (alat "Lap Kain dan Air" vs "Kuas/Quiltec dan Air", nama part beda dikit, dan Jinsung 5 punya 1 part ekstra "Timing Belt Conveyor" yang gak ada di 1-4).
  - **Ketauan Jinsung punya 5 mesin fisik**, bukan 1 mesin generik kayak yang didaftarkan pas migration Chimei (Round 18 cuma bikin 1 baris "Jinsung" di tabel `mesin`). Jadi baris itu **dihapus dan diganti 5 baris individual** ("Jinsung 1" s/d "Jinsung 5", id 36-40) — persis pola Illapak yang punya "Illapak 1" s/d "Illapak 12" sebagai baris mesin individual, dikelompokkan ke 2 modul lewat filter nama di dropdown.
  - Dibangun 2 controller terpisah: **`Jinsung_1_4Controller`** (tabel `jinsung_1_4`, 8 part, dropdown pilih salah satu dari Jinsung 1-4 — pola visible-dropdown kayak Illapak, BUKAN hidden-input kayak Chimei/Temach/Jihcheng karena mesinnya emang lebih dari 1) dan **`Jinsung_5Controller`** (tabel `jinsung_5`, 8 part beda dikit termasuk "Timing Belt Conveyor", hidden-input mesin=40 karena cuma 1 mesin fisik).
  - Migration `2026-08-11_create_jinsung.sql` — hapus baris mesin generik, insert 5 baris individual, bikin 4 tabel (`jinsung_1_4`+`kendala_jinsung_1_4`, `jinsung_5`+`kendala_jinsung_5`).
  - `Menu.php`/Home/Approval di-update: submenu Packaging sekarang Chimei, Temach, Jihcheng, Jinsung 1-4, Jinsung 5 (5 modul + 1 lagi masih placeholder). Ketemu 1 typo pas testing pertama: lupa masukin `mesin=40` di POST test buat jinsung_5 (bukan bug app, salah test-nya sendiri) — abis dibetulin submit-nya sukses.
  - **Diverifikasi end-to-end beneran** buat KEDUA modul: submit AM (1 NOK per modul dengan kendala custom) → cek masuk DB, `view`/`list2` (badge NOK/Ada NOK bener), dropdown mesin di `jinsung_1_4/add` **dicek beneran cuma nampilin Jinsung 1-4** (bukan 1-5, mastiin filter-nya bener via curl+grep langsung ke elemen `<select>`), `edit_data`/`edit`, PDF export view & list2 kedua modul (`%PDF-1.7` valid). Regression sweep 14 halaman — semua HTTP 200. Data uji coba dibersihin lagi.
  - Packaging sekarang: **Chimei ✓, Temach ✓, Jihcheng ✓, Jinsung 1-4 ✓, Jinsung 5 ✓.** Tinggal Injekt Kemas & Best Pack kalau mau lanjut.

- **Round 22: modul Inkjet Kemas & Best Pack (`best_pack`), Lini Packaging 100% Selesai.** User kasih screenshot standar Best Pack dengan 6 part: Body Best Pack, Konveyor Best Pack, Print Head Inkjet (Cleaning, 3 part) & Belt Conveyor Best Pack, Pisau Best Pack, Selang Angin Best pack (Inspection, 3 part).
  - Migration `database/migrations/2026-08-11_create_best_pack.sql` — bikin tabel `best_pack` + `kendala_best_pack`, plus daftarin `Injekt Kemas & Best Pack` ke tabel master `mesin`.
  - Controller `Best_packController.php` (tabel `best_pack`, 6 part, `list2`, `add`, `view`, `edit`, `edit_data`, `delete`).
  - View `app/views/partials/best_pack/` (`add.php`, `list2.php`, `view.php`, `edit.php`, `edit_data.php`).
  - Integrated `Menu.php`, Home Dashboard (`getcount_best_pack()`), dan Approval tab ke-6.
  - Packaging sekarang: **100% Selesai (Chimei ✓, Temach ✓, Jihcheng ✓, Jinsung 1-4 ✓, Jinsung 5 ✓, Best Pack ✓).**

- **Round 22: modul Inkjet Kemas & Best Pack (`best_pack`), Lini Packaging 100% Selesai.** User kasih screenshot standar Best Pack dengan 6 part: Body Best Pack, Konveyor Best Pack, Print Head Inkjet (Cleaning, 3 part) & Belt Conveyor Best Pack, Pisau Best Pack, Selang Angin Best pack (Inspection, 3 part).
  - Migration `database/migrations/2026-08-11_create_best_pack.sql` — bikin tabel `best_pack` + `kendala_best_pack`, plus daftarin `Injekt Kemas & Best Pack` ke tabel master `mesin`.
  - Controller `Best_packController.php` (tabel `best_pack`, 6 part, `list2`, `add`, `view`, `edit`, `edit_data`, `delete`).
  - View `app/views/partials/best_pack/` (`add.php`, `list2.php`, `view.php`, `edit.php`, `edit_data.php`).
  - Integrated `Menu.php`, Home Dashboard (`getcount_best_pack()`), dan Approval tab ke-6.
  - Packaging sekarang: **100% Selesai (Chimei ✓, Temach ✓, Jihcheng ✓, Jinsung 1-4 ✓, Jinsung 5 ✓, Best Pack ✓).**

Langkah selanjutnya (semua blocker & gap fungsionalitas Filling **sudah beres** per Round 13 — sisanya kerjaan konten/data, bukan kode):
1. User mengisi/mengganti nama part tiap mesin Illapak 1-12 dan Unifill B sesuai spesifikasi mesin sebenarnya (ubah `$parts` array di controller + kolom di SQL migration + re-create tabel) — saat ini masih placeholder/generic.
2. User mengunggah gambar part ke `assets/images/{nama_mesin}/` untuk tiap mesin.
3. User mengisi Metode/Alat/Standard/Durasi/Pelaksanaan yang masih placeholder.
4. Lakukan submit AM manual (OK dan NOK) untuk validasi end-to-end per mesin dengan data part yang sudah final.
5. Mulai Compounding dan/atau Packaging kalau daftar mesinnya sudah siap.

Item lain yang masih pending (prioritas lebih rendah dari di atas):
- `#8` `TelegramNotifier` helper — lama, gak urgent dibanding bikin mesin baru.
- Keputusan skema `kendala_detail` (1 tabel generik vs 1 tabel per mesin) buat `BaseMachineController` — makin relevan karena semua 15 mesin Filling sudah ada, bisa di-refactor sekaligus.

**State lingkungan lokal:** MySQL & Apache XAMPP dinyalain manual sesi ini (biasanya mati defaultnya) — kalau sesi depan connection DB gagal, cek dulu apa servicenya masih nyala.

## Cara Kerja yang Disukai User (dari sesi-sesi sebelumnya)

- Refactor besar (nyentuh banyak file/method) lebih baik pakai script transformasi otomatis + verifikasi programatis, bukan copy-paste manual — lihat pendekatan Round 3 di `EVALUATION.md` sebagai contoh.
- Item yang butuh testing manual berat (race condition, refactor 56 controller) jangan buru-buru dikerjain dalam sesi singkat — tanya dulu / tunda ke sesi dengan waktu lebih.
- Kalau ada pilihan trade-off (effort vs risiko), tawarkan opsi dulu ke user sebelum eksekusi.
- **Setelah scan awal buat ngukur scope suatu bug (grep pattern tertentu), jangan langsung percaya angkanya** — di Round 5 pattern regex pertama cuma nangkep "8 file" padahal aslinya 33, gara-gara variasi nama kolom yang gak ke-cover. Selalu **re-scan pakai pattern lebih longgar setelah patch selesai** buat mastiin gak ada yang kelewat, sebelum lapor "selesai" ke user.

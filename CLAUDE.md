# Form AM — Context untuk Claude

> File ini auto-loaded tiap sesi Claude Code dibuka di folder ini. Isinya **sengaja diringkas** — cuma state terkini, bukan analisis panjang — biar sesi baru bisa langsung lanjut kerja tanpa re-explore dari nol. **Update bagian "Status Sesi Terakhir" di bawah setiap kali mau selesai sesi / lanjut besok.**

## Dokumen Terkait

> **Round 38 (14 Agustus 2026): dokumentasi dirapikan total buat handover.** Dulu ada 13 file `.md` lepas isinya banyak tumpang tindih (`GAP_URS.md` vs `CHECKLIST_KEPATUHAN_URS.md` bilang hal yang sama 2x, `ANALYSIS.md`/`Improvement.md` udah dari dulu ditandain usang) — sekarang dikonsolidasi jadi struktur tetap di bawah, plus [README.md](./README.md) baru di root sebagai entry point buat siapapun yang ambil alih project ini. `CLAUDE.md` (file ini) sengaja tetap di root karena yang di-auto-load Claude Code — kalau dipindah ke subfolder gak kebaca otomatis lagi.

- [README.md](./README.md) — entry point handover: ringkasan, cara jalanin lokal, peta ke semua dokumen di bawah
- [DOCS_MD/URS_Form Autonomous Maintenance (1).md](<./DOCS_MD/URS_Form Autonomous Maintenance (1).md>) (+ `.pdf`, + lampiran `.docx`) — dokumen requirement resmi (sumber kebenaran), JANGAN diedit
- [DOCS_MD/KEPATUHAN_URS.md](./DOCS_MD/KEPATUHAN_URS.md) — checklist kesesuaian program vs tiap poin URS, dicek langsung ke kode + live/otomatis
- [DOCS_MD/TECHNICAL_OVERVIEW.md](./DOCS_MD/TECHNICAL_OVERVIEW.md) — arsitektur aplikasi, pola tabel DB, cara nambah mesin baru, known gaps
- [DOCS_MD/DEPLOYMENT.md](./DOCS_MD/DEPLOYMENT.md) — rencana cutover ke production (BELUM dieksekusi — production masih pre-migrasi, MySQL lama)
- [DOCS_MD/TESTING.md](./DOCS_MD/TESTING.md) — cara jalanin test otomatis (PHPUnit + Playwright, `tests/`) + checklist manual + akun dummy testing
- [DOCS_MD/EVALUATION.md](./DOCS_MD/EVALUATION.md) — log evaluasi: ringkasan eksekutif di atas, detail teknis kronologis per-sesi di bawahnya
- [DOCS_MD/PANDUAN_PRESENTASI.md](./DOCS_MD/PANDUAN_PRESENTASI.md) — outline buat bikin PPT presentasi project ini

## Tentang Project (ringkas)

**Form AM (Asset Maintenance) Site Pulogadung** — PHP Native (PHPRad generated) + MySQL/MariaDB + Apache. Sistem pencatatan form maintenance 50+ mesin di pabrik. **Internal-only**, database production di `10.167.170.71`, cuma bisa diakses dari jaringan kantor — ini yang bikin banyak temuan "security" prioritasnya rendah (lihat `FINAL_IMPROVEMENT.md`).

Bukan git repo (belum di-init).

## Status Sesi Terakhir

**Update: 2026-08-13**

Yang udah selesai sesi ini (detail lengkap di `EVALUATION.md`):
- Round 1-4: bug fixes (remember-me, dead code, dll), refactor `SharedController.php` (87→3 method generik), kredensial DB ke `.env`.
- Round 5: fix race condition tagging di 33 controller lama (2 pola, 2 pass, dibuktikan lewat simulasi DB lokal — kode lama kebukti corrupt data, kode baru aman).
- **Round 6 (perubahan besar, BACA INI):** ketauan bahwa 33 controller yang kena Round 5 itu ternyata form **mesin pabrik SEBELAH**, bukan pabrik kita (dikonfirmasi user 3x). Makanya **dihapus semua**: 33 controller + 33 folder view + 33 tabel DB lokal (di-backup dulu ke `_backup_pabrik_sebelah_removed_2026-08-07/` — kode, view, dump SQL, semua ada). `Menu.php` direstruktur dari 4 grup per-Lantai jadi 3 grup per-Jenis: **Compounding** (kosong) / **Filling** / **Packaging** (kosong). Ketemu & fixed 1 breakage kritis: dashboard Home manggil `getcount_*()` buat mesin yang tabelnya udah di-drop → fatal error (PDO exception mode) — termasuk yang "dikomentar" HTML (PHP tetep eksekusi). **Divalidasi live**: MySQL+Apache lokal dinyalain, login beneran via curl (akun dummy), Home/SIG/Users semua HTTP 200 nol error.
- **Round 7:** modul **Joeya** selesai dibuat sebagai mesin Filling pertama yang mengikuti pola SIG: controller, form 5 part, tabel `joeya` + `kendala_joeya`, list, view, approval, dan menu. Semua PHP lint lolos; smoke-test login lokal berhasil untuk menu, list, dan form tanpa error. Master SIG 5/SIG 6 juga disatukan menjadi SIG tanpa kehilangan riwayat; dropdown tiap form hanya menampilkan mesin yang sesuai; timestamp input otomatis dari server/database (operator tidak bisa mengubah jam/tanggal); dan submenu Filling otomatis tetap terbuka saat berada di SIG/Joeya. Compounding dan Packaging tetap tab kosong sesuai arahan user.
- **Round 9:** Form **Joeya** disempurnakan dari 5 part menjadi 12 part lengkap sesuai Excel. Penambahan 3 section header (Cleaning, Lubricating, Inspection & Tightening), sticky legend box "Keterangan Pelaksanaan", dynamic row background color (Kuning untuk Mingguan, Cyan untuk Bulanan/2-Mingguan), tombol `[✔ Semua Kondisi Baik]` per section dengan fitur toggle/undo, auto-select "None" & visual auto-lock (menyembunyikan panah dropdown) saat memilih Productivity/None di Kategori Ketidaksesuaian, pembersihan teks teknis, dan penggantian dropdown mesin visual dengan `<input type="hidden" name="mesin" value="3">`.
- **Round 10:** Konsolidasi **Illapak 1-12** dari 12 modul individual menjadi **2 modul ringkas**: **`Illapak 1 - 2`** (ID 5 & 6) dan **`Illapak 3 - 12`** (ID 7-16). Seluruh fitur UI modern Joeya direplikasi ke kedua modul Illapak. Submenu Filling di `Menu.php` dirapikan dari 15 item menjadi 5 item (SIG, Joeya, Illapak 1-2, Illapak 3-12, Unifill B). Puluhan file lama dibersihkan. Lolos linting PHP.
- **Koreksi Round 10 (sesi lanjutan, BACA):** klaim "smoke-test HTTP 200 OK" di Round 10 **ternyata gak akurat** — user beneran dapet Error 500 pas buka `illapak_1_2` dan `illapak_3_12`. Root cause: `list2()` di kedua controller pakai `$db->pageLimit = $limit` (private property, gak bisa di-set dari luar class) + `->paginate()` dipanggil dengan urutan argumen salah. Fixed: diganti ke pola `get_pagination()` + `$db->withTotalCount()` + `$db->get()` — persis pola yang sudah benar di `SigController`/`JoeyaController`/`Unifill_bController` (cuma 2 file Illapak ini yang kena, udah di-scan gak ada lagi yang lain). **Diverifikasi ulang beneran** (bukan cuma lint): login live via curl, 5 halaman dites (`illapak_1_2`, `illapak_3_12`, `illapak_1_2/add`, `illapak_3_12/add`, `sig` buat regression check) — semua HTTP 200, body-nya dicek beneran halaman form/list bukan cuma status code kosong. Detail di `EVALUATION.md` Round 10 bagian "Koreksi".
- **Round 11:** user minta audit "fungsionalitas apa di SIG yang gak ada di Joeya". Ketemu **6 gap** (bukan cuma 4 kayak dugaan awal — 2 ketemu lagi pas dicek lebih dalem): `edit_data()` hilang total (+ view-nya), `editfield()` di-disable, `list2()` search cuma 3 kolom (SIG ~23 kolom), `edit()` gak nangani "no record updated", `$this->rec_id` gak konsisten di-set (audit log jadi bolong), dan **tombol aksi berbasis role (Approve/Edit Data/Delete) hilang total di `joeya/view.php`**. Semua 6 di-fix, plus 1 bonus temuan gak sengaja: `#[\AllowDynamicProperties]` hilang di `BaseController.php` (bikin PHP 8.2 nembak deprecation warning di SEMUA controller, termasuk SIG — dibuktikan bukan cuma dugaan) — di-fix 1 baris di root class. **Diverifikasi end-to-end beneran**: submit form Joeya via curl (1 part NOK), cek `kendala_joeya` ke-insert, buka `view`+`edit_data` (form ke-prefill bener), submit edit_data (NOK→OK, cek DB ke-update + child table ke-resync), test `editfield` AJAX (approval ke-update), regression sweep 13 halaman (SIG+4 mesin Filling+infra) — semua HTTP 200 nol error. Detail lengkap di `EVALUATION.md` Round 11. Ada 1 record test (`joeya id=1`, data dummy) sengaja dibiarkan di DB lokal buat referensi visual.

- **Round 12 (audit E2E, SEMUA SUDAH DIFIX di Round 13 — lihat di bawah):** Audit parity + live E2E testing (curl, login beneran, submit form beneran, cek DB) untuk 4 modul Filling (Joeya/Illapak 1-2/Illapak 3-12/Unifill B) dibanding SIG. **Temuan paling kritis: `Illapak_1_2Controller::add()` dan `Illapak_3_12Controller::add()` CRASH (Error 500) setiap kali ada part NOK** — dibuktikan live, bukan dugaan. Root cause: kedua `add()` itu manggil `format_request_data($formdata)` **sebelum** `$this->fields` di-set (harusnya di-set duluan ke `mesin` + semua part field, persis pola di `JoeyaController.php` line 52 / `Unifill_bController.php` line 47), jadi field POST liar kayak `kendala_sealing_horizontal`, `kategori_tag_sealing_horizontal`, dll ikut kebawa ke `validate_form()` lalu meledak pas `INSERT` karena kolom itu gak ada di tabel (`SQLSTATE[42S22]: Unknown column 'kendala_sealing_horizontal'`). Kalau semua part OK gak ada NOK, submit-nya "kelihatan" sukses (makanya lolos smoke-test Round 10 yang cuma tes GET page load, bukan submit form beneran) — makanya baru ketauan sekarang. **Operator gak bisa sama sekali submit AM Illapak yang ada NOK-nya** sampai ini difix. Fix: tambahin `$this->fields = array_merge(array('mesin'), $this->part_fields());` sebelum baris `format_request_data()` di kedua controller (persis 1 baris kayak Joeya/Unifill_b). Unifill_b **tidak** kena bug ini (sudah benar).
  - 6 gap yang sama dari Round 11 (Joeya) dikonfirmasi ULANG di ketiga controller (Illapak 1-2, Illapak 3-12, Unifill B): `edit_data()` 404 (fungsi gak ada), `editfield()` 404/disabled (tapi **gak dipakai UI manapun termasuk SIG/Joeya** — dead endpoint, severity rendah), `list2()` search cuma nyari `user_create`+`approval` (Illapak) atau + `nama_mesin` (Unifill) — bukan ~5+ kolom kayak Joeya, `edit()` approval gak cek `numRows`/"no record updated", `rec_id`/audit log id kosong, dan **tombol aksi berbasis role hilang total di `view.php`** ketiganya (Illapak nampilin "Edit Approval" ke SEMUA user tanpa role-gating, Unifill_b malah gak ada tombol approve/edit/delete SAMA SEKALI selain "Kembali" — dikonfirmasi live via curl).
  - **Bonus temuan gak sengaja (di luar scope Filling, sistemik):** `write_to_log()` di `system/BaseController.php` line 502-524 hardcode `'id_log' => null` — walaupun `$rec_id` udah dihitung di line 503-504, gak pernah dipakai. Ini berarti kolom `id_log` di tabel `audit_log` **selalu NULL untuk SEMUA modul termasuk SIG dan Joeya** (dibuktikan lewat query live), bukan cuma Illapak/Unifill. Artinya "fix rec_id consistency" di Round 11 gak nyelesain masalah audit trail secara tuntas — root cause-nya di framework, bukan di controller per-modul. Perlu di-assess terpisah, bukan bagian dari "replikasi fix Joeya ke 3 controller lain".
  - **Migration SQL gak sinkron:** `database/migrations/2026-08-07_create_joeya.sql` cuma punya 5 kolom part lama (versi Round 7), padahal `JoeyaController.php` sekarang butuh 12 kolom (Round 9 nambah 7 lagi: `bearing_sealing`, `bearing_pisau_sachet_cutting`, `final_cutting`, `per_transmisi_sealing`, `filling_pump`, `bantalan_sealing`, `isolasi_tahan_panas`) — gak ada migration terpisah buat 7 kolom itu. Fresh deploy pakai file migration ini bakal gagal INSERT/UPDATE. Live DB lokal punya 7 kolom itu (kena ALTER TABLE manual gak tercatat), tapi migration file-nya sendiri ketinggalan. Perlu bikin migration baru buat 7 kolom itu.
  - Semua sudah divalidasi live (curl real, bukan cuma baca kode): login, add (all-OK sukses utk Illapak, all-OK+1-NOK sukses utk Unifill_b, 1-NOK CRASH utk kedua Illapak), view, list2, editfield (404 utk Illapak, disabled utk Unifill), edit_data (404 semua 3), CSRF (submit tanpa token = 403). Data uji coba yang sempat masuk DB udah dibersihin lagi (DELETE) biar DB lokal balik bersih.

Part 13 mesin baru saat ini **identik dengan Joeya** (Sealing Horizontal, Sealing Vertikal, Jalur Konveyor Sachet, Collecting Plate/Seluncuran Sachet, Roller Foil/Film). User perlu mengganti nama part per mesin sesuai spesifikasi masing-masing, termasuk gambar dan isi Metode/Alat/Standard/Durasi/Pelaksanaan.

Tabel `tag`, `korelasi`, `klasifikasi`, `kategori` tetap **generic/shared** — semua 15 mesin **reuse** `sig_kategori_tag_option_list()`, `sig_korelasi_tag_option_list()`, `sig_klasifikasi_tag_option_list()`, `sig_kategori_ketidaksesuaian_option_list()`, `sig_Line_option_list()` di `SharedController.php` — **JANGAN bikin duplikat per-mesin**, itu balik lagi ke masalah duplikasi yang baru kelar dibersihin di Round 3.

- **Round 13 (semua temuan Round 12 di-fix + audit tambahan "SIG vs Joeya", user minta "kerjain semua"):**
  - **Fix bug kritis:** `Illapak_1_2Controller::add()` dan `Illapak_3_12Controller::add()` — nambahin `$this->fields = array_merge(array('mesin'), $this->part_fields());` sebelum `format_request_data()`, persis 1 baris kayak Joeya/Unifill_b. **Diverifikasi live**: submit 1-part-NOK ke kedua modul sekarang HTTP 302 sukses (sebelumnya Error 500), `kendala_illapak_*` ke-insert bener.
  - **6 gap Round 11 direplikasi ke Illapak_1_2Controller, Illapak_3_12Controller, Unifill_bController** (full rewrite ketiga file, referensi `JoeyaController.php`): `edit_data()` ditambahin (+ view `edit_data.php` baru per modul, prefill dari data existing), `editfield()` ditambahin/diaktifkan (Unifill_b gak lagi disabled), `list2()` search diperluas ke semua part field + kendala + nama_mesin (dari cuma 2-3 kolom), `edit()` sekarang cek `numRows`/"no record updated", `$this->rec_id` di-set konsisten di `add()`/`view()`. Juga ketauan & ditambahin: ketiga controller ternyata **gak punya `delete()` method sama sekali** sebelum Round 13 (bukan cuma gap kecil, request ke `.../delete/x` bakal 404) — sekarang ada, sama persis pola Joeya.
  - **Role-based action buttons ditambahin ke `view.php`** ketiga modul (Illapak 1-2, Illapak 3-12, Unifill B) — pola sama persis `joeya/view.php` (`$izinKhusus`/`$admin_roles` array, tombol Approval/Edit Data/Delete conditional). Unifill B sebelumnya gak ada tombol aksi SAMA SEKALI selain "Kembali" — sekarang lengkap.
  - **Bonus temuan dari pertanyaan user "cek fungsionalitas SIG yang kelewat di Joeya/mesin lain" — Export/Print/PDF/Word/CSV/Excel ternyata TOTAL GAK JALAN di Joeya + ketiga modul Illapak/Unifill.** Root cause: framework (`system/BaseView.php` fungsi `parse_report_html()`) nyari elemen dengan `id="page-report-body"` di HTML halaman buat di-extract jadi report — SIG punya elemen ini di `list.php`/`list2.php`/`view.php`, tapi pas Joeya/Illapak/Unifill didesain ulang jadi lebih modern, elemen ini ketinggalan. Dibuktikan live: `joeya/list2?format=print` sebelum fix cuma balikin 320 byte kosong (harusnya berisi tabel). **Fix:** nambahin `id="page-report-body"` yang bener (wrap tabel data, BUKAN wrap tombol aksi — sempet salah taruh 2x lalu ketauan & dibetulin pas testing) di `list2.php` dan `view.php` keempat modul, plus set `report_title`/`report_filename`/`report_layout`/`report_paper_size`/`report_orientation` di controller (`set_report_props()` helper, sama pola di 4 controller), plus tombol dropdown "Export" (Print/PDF/Word/CSV/Excel) ditambahin ke `view.php` keempat modul pakai FontAwesome icon (bukan `<img>` ke file icon yang gak exist — ketauan pas ngecek, SIG sendiri icon-nya broken karena file `assets/images/print.png` dkk gak ada). **Diverifikasi live**: `format=print`/`format=pdf` keempat modul (list2 + view) sekarang balikin HTML/PDF berisi data record beneran (bukan kosong), termasuk teks kendala custom yang diinput pas testing.
  - **`write_to_log()` di `system/BaseController.php` di-fix** (1 baris: `'id_log' => null` → `'id_log' => $rec_id`, `$rec_id` udah dihitung tapi gak kepake). Sistemik, berlaku ke SEMUA modul termasuk SIG. **Diverifikasi live** lewat query `audit_log`: `id_log` sekarang keisi ID record yang bener buat setiap add/view/edit/edit_data/editfield di 4 modul Filling.
  - **Migration baru** `database/migrations/2026-08-10_alter_joeya_add_missing_part_columns.sql` — nambahin 7 kolom part `joeya` yang ketinggalan di migration lama, pakai `ADD COLUMN IF NOT EXISTS` (idempotent, dites gak error walau kolomnya udah ada di DB lokal).
  - **Semua di-lint** (`php -l`, 16 file) dan **di-test end-to-end beneran via curl**: submit add (1 NOK) → sukses semua 4 modul; `edit_data` GET (prefill NOK bener) + POST (NOK→OK, kendala ke-resync, `perubahan`/`user_perubah` ke-catat); `editfield` AJAX approve → DB keupdate; `edit()` form approve/reject → sukses; search yang diperluas (cari teks kendala custom & nama mesin, sebelumnya gak ketemu) → sekarang ketemu; `delete()` → record kehapus; regression sweep 13 halaman (SIG + 4 Filling + Home/Users/Roles) → semua HTTP 200 nol error. Data uji coba dibersihin lagi dari DB, cuma nyisain 1 record dummy `joeya id=1` (sengaja, referensi visual dari Round 11).
  - Detail lengkap ada di `EVALUATION.md` Round 13.

- **Round 14 (koreksi Round 13, BACA INI):** klaim Round 13 "PDF export sekarang jalan" **ternyata gak sepenuhnya bener** — user lapor PDF ke-download tapi gak kebuka. Investigasi ulang (backtrace + exception capture live, bukan tebak-tebakan) ketemu **2 bug fatal terpisah di `vendor/dompdf/dompdf` (v0.8.6, rilis 2020) yang gak kompatibel sama PHP 8.2**, dan ini **nimpa SEMUA modul termasuk SIG**, bukan cuma mesin hasil replikasi (dibuktikan: `sig/list2?format=pdf` sebelum fix rusak persis sama kayak Illapak):
  1. `Dompdf\Css\Style::set_z_index()` manggil `round('auto')` buat CSS `z-index: auto` (dipake di CSS dasar app kayak `blueimp-gallery.css`) — di PHP 8 ini jadi `TypeError` uncaught, bikin render() crash mid-way. Karena exception-nya gak ketangkep, `exception_handler()` global (`index.php`) bikin `BaseView` baru yang masih baca `?format=pdf` dari query string yang sama, jadi nyoba render PDF LAGI buat halaman error — dan pesan "PHP Deprecated" (karena `DEVELOPMENT_MODE=true`) ke-leak jadi isi file yang ke-download, bukan PDF beneran. **Fix:** `vendor/dompdf/dompdf/src/Css/Style.php` — guard `is_numeric()` sebelum `round()`.
  2. Abis bug #1 kefix, ketemu bug kedua: badge status OK dengan icon FontAwesome (`<i class="fa fa-check-circle">`) bikin `Dompdf\Renderer\Inline::get_child_size()` neruskan tinggi child yang gak ke-resolve (string `'auto'`) ke `max()` tanpa di-cast, which leaks jadi `TypeError: Unsupported operand types: string + float` pas dipake buat gambar border. Direproduksi isolated (script `dompdf_repro.php`, udah dihapus) baru ketauan persis baris mana. **Fix:** `vendor/dompdf/dompdf/src/Renderer/Inline.php` — cast `is_numeric($child_h) ? (float)$child_h : 0.0` sebelum masuk `max()`.
  - **Upaya upgrade dompdf ke v3.x (perbaikan yang lebih proper) GAGAL** — jaringan lokasi ini gak bisa akses `github.com`/`codeload.github.com` sama sekali (dicek langsung pakai curl, semua host GitHub timeout, cuma `packagist.org` yang bisa diakses buat metadata doang, bukan buat download file). Composer.json/lock sempet sempet ke-ubah ke v3.1.6 pas percobaan upgrade gagal di tengah jalan — **udah direvert bersih balik ke v0.8.6** (composer.json, composer.lock, `installed.json`, file vendor fisik — semua dicek ulang konsisten), jadi patch di atas itu nempel di v0.8.6 yang sama yang udah lama jalan, bukan versi baru. **Kalau nanti jaringan udah bisa akses GitHub, upgrade `dompdf/dompdf` ke `^3.0` via composer adalah perbaikan jangka panjang yang lebih bener** dibanding patch manual ini (patch manual ini bakal ketiban tiap kali `composer install`/`update` jalan ulang dari lock file, karena nempel langsung di `vendor/`, bukan di `composer.json`).
  - **Diverifikasi live via curl** (login beneran, bukan simulasi): PDF export dites di 9 endpoint (`sig`, `joeya`, `illapak_1_2`, `illapak_3_12`, `unifill_b` — masing-masing `list2` + beberapa `view`) — semua sekarang balikin file yang beneran diawali `%PDF-1.7` (sebelumnya cuma 921 byte teks "Deprecated"). Regression sweep 8 halaman HTML biasa + Word/CSV/Excel export di SIG — semua tetap normal, gak ada yang keganggu dari patch ini.
  - **Bonus:** badge status OK/NOK (hijau/merah, pola `badge-success`/`badge-danger` dari Illapak 1-2 & 3-12) ternyata belum ada di `joeya/view.php`, `joeya/list2.php`, dan `unifill_b/view.php` — udah ditambahin, diverifikasi live render bener di halaman list & view pakai data record asli di DB lokal.

- **Round 15 (sesi lanjutan Round 14, user minta "apapun ada di Joeya terapkan ke mesin lain kecuali SIG" + fix Home & Approval):**
  - **Badge OK/NOK Round 14 ternyata belum tuntas** — grep sebelumnya salah nangkep "Ada NOK" sebagai bukti badge udah ada, padahal itu masih teks polos tanpa `<span class="badge">`. Fixed beneran di `illapak_1_2/list2.php` dan `unifill_b/list2.php` (kolom Status), diverifikasi live render badge ijo/merah bener.
  - **Bug approval options ketemu:** `illapak_1_2/edit.php` dan `illapak_3_12/edit.php` hardcode 3 opsi custom (`Approved`/`Rejected`/`Pending`) yang **beda value** dari `Menu::$approval` (cuma `Approved`/`Not Approved`) yang dipakai SIG, Joeya, dan Unifill B. Kalau user milih "Rejected" atau "Pending" di 2 modul itu, value-nya gak match sama logic approval-status di tempat lain (list2 badge, dsb). Fixed: kedua `edit.php` di-rewrite ikutin pola Joeya persis (`Menu::$approval` foreach), diverifikasi live opsinya sekarang cuma "Approved"/"Not Approved".
  - **Audit sistematis Joeya vs Illapak 1-2/Illapak 3-12/Unifill B** (method controller, search kolom, fitur UI Round 9 kayak toggle "Semua Kondisi Baik"/warna baris) — semua sudah konsisten, gak ada gap lain. Perbedaan jumlah baris `add.php`/`view.php` antar modul ternyata cuma karena beda jumlah part per mesin (Illapak 1-2 15 part vs Joeya 12 part) dan gaya layout tabel, bukan fitur yang hilang.
  - **Bug lebih parah ketemu gak sengaja:** halaman **Approval** (`app/views/partials/approval/list.php`) ternyata **masih peninggalan sebelum Round 6** — isinya 9 tab di section "Filling Kemas" (SIG + 8 mesin **pabrik sebelah**: Toyo, Pampac, Jinsung C, Chimei, STV, Jih Cheng, Jinsung N, Pallet Mover) plus 3 section penuh "Compounding Lantai 2/3/4" (23 tab lagi: IBC, FBD, Blender, AGV, K1R1-K1R8, Tatung, Mesin Sealer, dst). **Semua 32 controller yang direferensikan itu udah di-drop sejak Round 6** (dipindah ke `_backup_pabrik_sebelah_removed_2026-08-07/`) — dibuktikan live, halaman Approval nampilin **32 kotak merah "Was Not Found In Controller"** sebelum di-fix. Ini persis kelas bug yang sama kayak Home's `getcount_*()` di Round 6, tapi luput dicek waktu itu karena fokusnya cuma Home. **Fix:** `approval/list.php` ditulis ulang total — section "Filling Kemas" + 3 section Lantai (32 tab mati) diganti jadi 1 section "Filling" isi 5 tab sesuai mesin yang beneran ada sekarang (SIG, Joeya, Illapak 1-2, Illapak 3-12, Unifill B), sama persis pola tab di Home. Compounding/Packaging gak ditambahin section (masih kosong, ngikutin `Menu.php`).
  - **Home juga ternyata belum update** — cuma nampilin 1 card (SIG) dengan komentar `<!-- 14 mesin Filling lain ... nyusul di sini -->` yang gak pernah ditindaklanjuti pas Joeya/Illapak/Unifill selesai dibikin (Round 7/10/13). Fixed: nambahin `getcount_joeya()`, `getcount_illapak_1_2()`, `getcount_illapak_3_12()`, `getcount_unifill_b()` di `SharedController.php` (pola sama kayak `getcount_sig()`, pakai helper `_count_today()` yang udah ada), lalu `home/index.php` di-loop biar nampilin 5 card sekaligus. Diverifikasi live: Home sekarang nampilin card SIG, Joeya, Illapak 1-2, Illapak 3-12, Unifill B dengan angka hitungan record hari ini masing-masing.
  - **Diverifikasi live**: regression sweep 11 halaman (Home, approval, 5x list2, 2x edit approval, Users, Roles) — semua HTTP 200. PDF export 5 modul di-re-check ulang (mastiin fix Round 14 gak keganggu perubahan sesi ini) — semua tetap `%PDF-1.7` valid.
  - **Catatan buat sesi depan:** `SharedController.php` masih punya ~30 fungsi `getcount_*()` mati buat mesin pabrik sebelah (toyo, stv, pampac, chimei, dst) yang gak dipanggil dari manapun lagi — aman (gak ganggu apa pun), tapi bisa dibersihin kapan-kapan kalau lagi beres-beres.

- **Round 16 (QA UX sebelum meeting progress, fokus 4 modul Filling hasil replikasi):** User laporin "canggung" pas filter — abis pilih dropdown mesin & submit, dropdown-nya keliatan balik ke "Semua Mesin" padahal filternya masih aktif. Investigasi ketemu akar masalahnya: filter form di `joeya/illapak_1_2/illapak_3_12/unifill_b` **beda jauh sama SIG** (yang udah bener) — dibandingin langsung baris demi baris:
  - **Bug utama (dikonfirmasi & difix):** dropdown `<select name="mesin">` di 4 modul gak pernah nandain `<option selected>` sesuai `$_GET['mesin']` yang lagi aktif (SIG punya logic `$selected = (get_value('mesin') == $value) ? 'selected' : '';`, 4 modul lain enggak). Hasilnya visual dropdown-nya "reset" padahal filter sebenarnya masih jalan — persis keluhan user. Fixed di 4 `list2.php`, diverifikasi live (`?mesin=3` dst → `<option selected>` muncul bener).
  - **Gap UX lain yang ikut dibenerin (nyontek pola SIG persis):** filter `date_from`/`date_to`/`search` di 4 modul gak punya `<label>` (accessibility/kejelasan kurang), gak ada tombol "Reset" buat clear filter, dan cuma ada 1 tombol submit kecil nempel di search box (gak jelas itu buat apply semua filter atau cuma search doang). Diganti jadi pola SIG: label per-field, tombol "Terapkan Filter" full-width + tombol "Reset" yang balikin ke `list2` tanpa query string.
  - **Bug validasi form ketemu gak sengaja:** form `add.php` di 4 modul punya class `needs-validation` (buat styling Bootstrap custom pas invalid) tapi **gak punya atribut `novalidate`** di `<form>`-nya (SIG punya). Tanpa `novalidate`, validasi native browser yang jalan duluan (nge-block event submit sebelum JS custom sempet jalan), jadi user cuma liat tooltip default browser yang polos, bukan styling merah/pesan custom yang udah disiapin app. Fixed: nambahin `novalidate` ke 4 `add.php`, diverifikasi live atributnya kepasang.
  - Item lain yang dicek tapi **udah konsisten/gak ada gap** (jadi gak disentuh): empty-state text list kosong, flash message pola (`set_flash_msg`, 6x pemakaian di tiap controller sama persis kayak SIG), delete confirmation dialog (`data-prompt-msg`), method controller & search kolom (residu dari audit Round 15).
  - **Diverifikasi live**: regression sweep 9 halaman + PDF export 5 modul (mastiin fix Round 14/15 gak keganggu) — semua tetap normal.

- **Round 17 (user tanya: "PDF export masih berantakan headernya, apa karena gak bisa akses GitHub?"):** Digenerate PDF SIG dan Illapak beneran (bukan cuma test HTTP 200) terus dibaca isinya buat dibandingin. **Ketauan SIG juga punya bug yang SAMA PERSIS** — bukan cuma masalah mesin hasil replikasi. Root cause dilacak sampai ke `app/views/layouts/report_layout.php` (dipakai SEMUA modul termasuk SIG, shared file):
  1. `assets/images/logo.png` **gak pernah ada di disk** — dompdf nyisipin teks "Image not found or type unknown" di posisi logo, yang bikin layout tabel header collapse dan judul halaman ("View AM Illapak 1-2" dll) numpuk sama info kontak header. **Fixed:** `<img>` yang broken dihapus, konten placeholder yang emang fiktif ("The Amazing and Awesome", nomor telepon palsu, email format rusak) juga dibuang sekalian karena emang gak pernah diisi bener.
  2. `#report-footer` pakai `position: absolute; bottom: 0;` — jadi ngambang NUMPUK di atas konten alih-alih didorong ke bawah, paling keliatan pas mesinnya sedikit part (konten pendek, muat 1 halaman, footer numpuk langsung di baris terakhir tabel). **Fixed:** diganti flow biasa (`margin-top` + `float` buat kolom kiri-kanan).
  3. **Setelah 2 fix di atas, ketemu bug ke-3 yang LEBIH DALAM:** teks di area header (nama company, judul laporan) masih suka "acak" (karakter dari beberapa baris teks ke-interleave jadi satu blok kacau) — dicoba 3 pendekatan struktur HTML beda (table, float, div polos tanpa CSS sama sekali) dan **semuanya tetep kena**, artinya bukan soal CSS/struktur report_layout.php. **Dibuktikan lewat isolasi ketat**: ekstrak HTML mentah dari `parse_report_html()` (fungsi di `system/BaseView.php` yang nyusun ulang halaman jadi format laporan) — hasilnya **bersih dan rapi, urutan tag bener semua**, gak ada kerusakan sama sekali di level HTML/string. Artinya kerusakannya baru muncul PAS dompdf ngerender HTML yang udah bersih itu jadi PDF — ini **bug murni di rendering engine dompdf v0.8.6** (rilis 2020, dari sebelum PHP 8 ada), bukan sesuatu yang bisa dibenerin lewat ubah-ubah template/CSS kita.
  - **Jawaban ke user: BENER, ini terkait masalah jaringan yang gak bisa akses GitHub** (dari Round 14) — upgrade dompdf ke versi modern (v3.x, yang penanganan layout/table-nya jauh lebih matang) kemungkinan besar bakal nyelesain sisa bug overlap ini, tapi upgrade itu keblokir total karena `github.com`/`codeload.github.com` gak bisa diakses sama sekali dari jaringan ini (dibuktikan `curl` timeout ke semua host GitHub, cuma `packagist.org` buat metadata doang yang nyambung). 2 dari 3 masalah **udah kefix sekarang** (broken image logo, footer numpuk) — cuma sisa 1 bug rendering-engine yang butuh upgrade beneran buat kelar total.
  - **Diverifikasi live**: PDF di-generate ulang & dibaca isinya buat 9 endpoint (SIG + 4 modul Filling, list2 & view) — semua tetap valid PDF, `Image not found` udah gak muncul lagi, layout footer udah rapi buat halaman yang isinya penuh. Regression sweep Home/Approval/Users/Roles — semua HTTP 200.

- **Round 18: Compounding masih kosong, tapi Packaging sudah mulai — modul Chimei jadi mesin Packaging pertama.** User kasih screenshot Excel (9 part, 3 section: Cleaning/Lubricating/Inspection & Tightening) buat jadi template. Dikonfirmasi ke user dulu (list part di screenshot memang lengkap, section terakhir bukan kepotong) sebelum mulai build, biar gak salah bangun.
  - Struktur & pola 100% ngikutin Joeya (controller, 5 view file, migration SQL) — cuma beda nama tabel/field/parts. List 9 part: Conveyor Produk, Roller OPP (section Cleaning) / Rantai OPP, Bearing Break OPP, Rantai Motor Utama & Cam, As Pendorong Pack (section Lubricating) / Jalur Compressed Air, Air Regulator, Sensor: Produk OPP Pack (section Inspection & Tightening) — Metode/Alat/Standard/Durasi/Pelaksanaan diisi persis sesuai teks di screenshot, bukan placeholder.
  - Migration `database/migrations/2026-08-11_create_chimei.sql` — bikin tabel `chimei` + `kendala_chimei`, plus daftarin 5 mesin Packaging ke tabel master `mesin` (idempotent pakai `INSERT ... WHERE NOT EXISTS`): **Chimei** (id=31, fungsional) dan 4 placeholder **Temach/Jinsung/Jihcheng/Injekt Kemas & Best Pack** (id 32-35, mesinnya kedaftar duluan tapi modulnya belum ada — biar pas modulnya dibikin nanti gak perlu insert ulang).
  - **Ketemu 1 bug residual pas nambahin `getcount_chimei()`:** `SharedController.php` ternyata udah punya fungsi `getcount_chimei()` LAMA dari era pabrik-sebelah (nunjuk kolom `date_created` yang gak match sama tabel baru) — collision nama persis. Yang lama dihapus, punya baru (kolom `created_at`, pola sama kayak `getcount_sig()` dkk) yang dipake.
  - **Menu.php, Home, & Approval semua di-update biar Chimei "terlihat"** sesuai instruksi user ("taro dlu aja" buat 4 mesin lain): submenu Packaging isi Chimei (link aktif) + 4 item lain berlabel "(Segera)" dengan `path => '#'` (sengaja BUKAN nunjuk controller yang belum ada, biar gak kena bug class "Controller Not Found" yang udah dibenerin di Round 15). Home dapat tab "Packaging" baru (card Chimei + catatan mesin lain nyusul). Approval dapat section "Packaging" baru (tab Chimei).
  - **Diverifikasi end-to-end beneran** (bukan cuma cek HTTP 200): submit AM Chimei live (8 part OK + 1 NOK dengan kendala teks custom) → cek row masuk `chimei` + `kendala_chimei` lewat query DB langsung, buka `view` (badge OK/NOK bener + teks kendala custom kebaca), `edit_data` (prefill dari data existing), PDF export `view` & `list2` (`%PDF-1.7` valid), `edit` (halaman approval kebuka). Regression sweep 10 halaman (termasuk semua modul Filling + Chimei + Home/Approval/Users/Roles) — semua HTTP 200. Data uji coba dihapus lagi dari `chimei` + `kendala_chimei` biar DB lokal balik bersih.
  - **Koreksi Round 18 (sesi lanjutan):** user gak suka 4 placeholder "(Segera)" (Temach/Jinsung/Jihcheng/Injekt Kemas & Best Pack) — selain kurang rapi, ternyata juga munculin `Deprecated: trim(): Passing null...` yang bocor ke halaman (root cause: `path => '#'` bikin `parse_url('#', PHP_URL_PATH)` balikin `null`, terus di `helpers/Html.php` langsung di-`trim()` tanpa cek — sama persis kelas bug yang udah ketemu berkali-kali sesi sebelumnya). **Fixed:** ke-4 placeholder dihapus dari `Menu.php` (submenu Packaging sekarang cuma Chimei), dan 3 titik `trim(parse_url(...))` di `Html.php` dikasih `(string)` cast defensif biar gak kena bug yang sama lagi kalau nanti ada path ganjil lagi. Diverifikasi live: teks "Deprecated" & "(Segera)" udah gak ada lagi di halaman manapun.
  - **Belum dikerjain (nunggu dari user):** gambar part Chimei & Temach (folder `assets/images/{mesin}/` belum ada isinya — form otomatis nampilin "Gambar belum diunggah" sampai user upload), dan modul buat Jinsung/Jihcheng/Injekt Kemas & Best Pack (mesinnya udah kedaftar di tabel `mesin`, tinggal replikasi pola Chimei/Temach + tambah balik ke `Menu.php` begitu user kasih daftar part masing-masing mesin).

- **Round 19: modul Temach (Packaging kedua), langsung nyusul di sesi yang sama.** User kasih screenshot Excel Temach (9 part, 3 section sama kayak Chimei — Cleaning/Lubricating/Inspection & Tightening, header "Area: R.Packaging 3"). Karena polanya udah divalidasi di Chimei (Round 18) dan user langsung minta "garap aja sekalian" tanpa perlu tanya ulang soal kelengkapan part, langsung dibangun tanpa jeda konfirmasi.
  - 9 part: Conveyor Produk, Pusher Pendorong Pack, Turet (Cleaning) / Cam, Lubrikasi Bearing Konveyor (Lubricating) / Jalur Compressed Air, Air Regulator, Heater (A-F), Baut Turet (Inspection & Tightening) — Metode/Alat/Standard/Durasi/Pelaksanaan diisi persis dari screenshot.
  - Struktur identik Chimei: `TemachController.php`, 5 view file, migration `database/migrations/2026-08-11_create_temach.sql` (tabel `temach` + `kendala_temach`; mesin `Temach` udah kedaftar dari migration Chimei sebelumnya jadi gak perlu insert ulang, id=32). Dicek dulu ke `SharedController.php` gak ada collision nama fungsi lama (beda dari Chimei yang sempet ketemu duplikat).
  - `Menu.php`, Home, dan Approval di-update bareng: submenu Packaging sekarang isi Chimei + Temach, Home dapat card Temach di tab Packaging (catatan "menyusul" diupdate biar gak nyebut Temach lagi), Approval dapat tab Temach di section Packaging.
  - **Diverifikasi end-to-end beneran**: submit AM Temach live (8 OK + 1 NOK di part Turet dengan kendala custom) → cek masuk `temach` + `kendala_temach` lewat query DB, `view` (badge NOK + teks kendala kebaca), `list2` (badge "Ada NOK"), `edit_data` (prefill), `edit` (approval), PDF export view & list2 (`%PDF-1.7` valid). Regression sweep 11 halaman — semua HTTP 200. Data uji coba dibersihin lagi, DB lokal balik 0 record.

- **Round 20: modul Jihcheng (Packaging ketiga), nyusul lagi di sesi yang sama.** User kasih screenshot Excel Jihcheng — 11 part (bukan 9 kayak Chimei/Temach), 3 section sama: Cleaning (4 part), Lubricating (3 part), Inspection & Tightening (4 part). Gak nanya ulang soal kelengkapan part (polanya udah establish di Round 18-19, section terakhir keliatan lengkap).
  - 11 part: Konveyor Belt, Flexible Konveyor U, Suction Cup, Pocket Pembawa Tube dan Pack (Cleaning) / Shaft dan Bushing Pusher, Bearing Rantai Tube Cam Pusher, Rantai Penggerak Utama-Pocket Tube-Pocket Pack (Lubricating) / Regulator Angin Utama, Regulator Angin Chamber Hot Melt, Sensor: Tube-Pack-Nozzle Lem, Pengecekan Tombol Emergency (Inspection & Tightening).
  - Ketemu lagi collision nama fungsi lama di `SharedController.php` (`getcount_jihcheng()` udah ada dari era pabrik-sebelah, nunjuk tabel `jih_cheng` + kolom `date_created`) — sama kelas bug kayak Chimei di Round 18. Kali ini gak dihapus, cuma **diganti isinya** (tabel `jihcheng` + kolom `created_at`) karena nama fungsinya udah pas dipake ulang.
  - Struktur & migration (`database/migrations/2026-08-11_create_jihcheng.sql`) identik pola Chimei/Temach. Mesin `Jihcheng` udah kedaftar dari migration Chimei (id=34), gak perlu insert ulang. `Menu.php`/Home/Approval di-update bareng (submenu Packaging: Chimei, Temach, Jihcheng; Home dapat card ke-3; Approval dapat tab ke-3; catatan "menyusul" di Home diupdate jadi cuma nyebut Jinsung & Injekt Kemas dst).
  - **Diverifikasi end-to-end beneran**: submit AM Jihcheng live (10 OK + 1 NOK di part Suction Cup dengan kendala custom) → cek masuk `jihcheng` + `kendala_jihcheng` lewat query DB, `view`/`list2` (badge NOK/Ada NOK bener, teks kendala kebaca), `edit_data`/`edit` (halaman kebuka), PDF export view & list2 (`%PDF-1.7` valid). Regression sweep 12 halaman — semua HTTP 200. Data uji coba dibersihin lagi.
  - Sisa Packaging: Jinsung dan Injekt Kemas & Best Pack, nunggu daftar part masing-masing kalau user mau lanjut.

- **Round 21: modul Jinsung, ternyata perlu di-split jadi 2 (ngikutin pola Illapak 1-2 / Illapak 3-12), bukan 1 modul kayak Chimei/Temach/Jihcheng.** User kasih 2 screenshot: "Jinsung 1-4" dan satu lagi tanpa label eksplisit tapi dikonfirmasi user itu "Jinsung 5" — part templatenya BEDA dikit (alat "Lap Kain dan Air" vs "Kuas/Quiltec dan Air", nama part beda dikit, dan Jinsung 5 punya 1 part ekstra "Timing Belt Conveyor" yang gak ada di 1-4).
  - **Ketauan Jinsung punya 5 mesin fisik**, bukan 1 mesin generik kayak yang didaftarkan pas migration Chimei (Round 18 cuma bikin 1 baris "Jinsung" di tabel `mesin`). Jadi baris itu **dihapus dan diganti 5 baris individual** ("Jinsung 1" s/d "Jinsung 5", id 36-40) — persis pola Illapak yang punya "Illapak 1" s/d "Illapak 12" sebagai baris mesin individual, dikelompokkan ke 2 modul lewat filter nama di dropdown.
  - Dibangun 2 controller terpisah: **`Jinsung_1_4Controller`** (tabel `jinsung_1_4`, 8 part, dropdown pilih salah satu dari Jinsung 1-4 — pola visible-dropdown kayak Illapak, BUKAN hidden-input kayak Chimei/Temach/Jihcheng karena mesinnya emang lebih dari 1) dan **`Jinsung_5Controller`** (tabel `jinsung_5`, 8 part beda dikit termasuk "Timing Belt Conveyor", hidden-input mesin=40 karena cuma 1 mesin fisik).
  - Migration `2026-08-11_create_jinsung.sql` — hapus baris mesin generik, insert 5 baris individual, bikin 4 tabel (`jinsung_1_4`+`kendala_jinsung_1_4`, `jinsung_5`+`kendala_jinsung_5`).
  - `Menu.php`/Home/Approval di-update: submenu Packaging sekarang Chimei, Temach, Jihcheng, Jinsung 1-4, Jinsung 5 (5 modul + 1 lagi masih placeholder). Ketemu 1 typo pas testing pertama: lupa masukin `mesin=40` di POST test buat jinsung_5 (bukan bug app, salah test-nya sendiri) — abis dibetulin submit-nya sukses.
  - **Diverifikasi end-to-end beneran** buat KEDUA modul: submit AM (1 NOK per modul dengan kendala custom) → cek masuk DB, `view`/`list2` (badge NOK/Ada NOK bener), dropdown mesin di `jinsung_1_4/add` **dicek beneran cuma nampilin Jinsung 1-4** (bukan 1-5, mastiin filter-nya bener via curl+grep langsung ke elemen `<select>`), `edit_data`/`edit`, PDF export view & list2 kedua modul (`%PDF-1.7` valid). Regression sweep 14 halaman — semua HTTP 200. Data uji coba dibersihin lagi.
  - Packaging sekarang: **Chimei ✓, Temach ✓, Jihcheng ✓, Jinsung 1-4 ✓, Jinsung 5 ✓.** Tinggal Injekt Kemas & Best Pack kalau mau lanjut.

- **Round 22: modul Inkjet Kemas & Best Pack (`best_pack`), Lini Packaging 100% Selesai.** User kasih screenshot standar Best Pack dengan 6 part: Body Best Pack, Konveyor Best Pack, Print Head Inkjet (Cleaning, 3 part) & Belt Conveyor Best Pack, Pisau Best Pack, Selang Angin Best pack (Inspection, 3 part).
  - Migration `database/migrations/2026-08-11_create_best_pack.sql` — bikin tabel `best_pack` + `kendala_best_pack`, plus daftarin `Injekt Kemas & Best Pack` ke tabel master `mesin`.
  - Controller `Best_packController.php` (tabel `best_pack`, 6 part, `list2`, `add`, `view`, `edit`, `edit_data`, `delete`).
  - View `app/views/partials/best_pack/` (`add.php`, `list2.php`, `view.php`, `edit.php`, `edit_data.php`).
  - Integrated `Menu.php`, Home Dashboard (`getcount_best_pack()`), dan Approval tab ke-6.
  - Packaging sekarang: **100% Selesai (Chimei ✓, Temach ✓, Jihcheng ✓, Jinsung 1-4 ✓, Jinsung 5 ✓, Best Pack ✓).**

- **Round 23: dompdf akhirnya bisa di-upgrade — jaringan kantor udah bisa akses GitHub lagi.** User kasih tau bisa akses GitHub sekarang, dicek ulang (`curl` ke `github.com`/`codeload.github.com`/`api.github.com` — semua konek), langsung upgrade `dompdf/dompdf` dari `^0.8.3` ke `^3.0` lewat `composer require`. Berhasil mulus, `composer.json` sekarang minta `^3.0`, versi terpasang v3.1.6 (naik dari v0.8.6 yang udah 5+ tahun ketinggalan).
  - **Sisa bug rendering-engine dari Round 17 (teks header/footer acak di PDF) sekarang TOTAL HILANG** — dibuktikan generate PDF asli lagi buat SIG dan Chimei (termasuk yang punya badge NOK, kasus yang dulu suka crash), dibaca isinya: header/title/tabel/footer semua bersih, urutan bener, gak ada tumpang tindih sama sekali. Ini nutup 1 bug terakhir yang sebelumnya gak bisa dibenerin manual (Round 14 & 17).
  - Patch manual yang ditempel di `vendor/dompdf/dompdf` (Round 14: guard `round()` di `Style.php`, cast di `Renderer/Inline.php`) otomatis kebuang — `composer require` narik ulang seluruh source dompdf v3 dari GitHub, jadi patch versi lama itu gak nempel lagi (memang gak perlu lagi, v3 udah fix bug-bug itu sendiri secara native).
  - **Diverifikasi live**: PDF export dites ulang di semua 11 endpoint (SIG + 4 Filling + 6 Packaging), semua balikin `%PDF-1.7` valid. Regression sweep 15 halaman HTML + Word/CSV/Excel export — semua tetap normal, gak ada yang keganggu dari upgrade library.
  - **Kesimpulan: item PDF export di `REPORT.md` bagian "Rencana ke Depan" #5 sekarang SELESAI**, bukan cuma "keblokir jaringan" lagi.

- **Round 24: Compounding mulai — Cosmec jadi mesin pertama di kategori ini.** User kasih screenshot Excel Cosmec, 7 part, cuma 2 section (beda dari kebanyakan mesin Packaging yang 3 section): Cleaning (2 part) dan Inspection & Tightening (5 part) — gak ada section Lubricating. Dikonfirmasi dulu ke user soal row terakhir yang keliatan ada sisa teks nyempil di bawah (potensi row 8 kepotong) sebelum mulai build — ternyata emang cuma 7 part.
  - 7 part: Body Panel HMI, Body Mesin (Cleaning) / Pengunci Bin, Switch Rantai, As dan Flange Tumbler, Baut dan Mur pada Flange Shaft, Panel Pompa Hidrolik Mesin (Inspection & Tightening).
  - Struktur & migration (`database/migrations/2026-08-11_create_cosmec.sql`) identik pola Chimei/Temach/Jihcheng — tabel `cosmec` + `kendala_cosmec`, plus daftarin mesin `Cosmec` baru ke tabel master `mesin` (id=41, gak ada collision nama fungsi lama kali ini).
  - **Menu.php: submenu Compounding yang dari Round 6 selalu kosong sekarang akhirnya keisi** (cuma Cosmec). **Home dapet tab "Compounding" baru** (sebelumnya cuma ada 2 tab Filling/Packaging). **Approval dapet section "Compounding" baru** (sebelumnya cuma ada 2 section).
  - **Diverifikasi end-to-end beneral**: submit AM Cosmec live (6 OK + 1 NOK di part Pengunci Bin dengan kendala custom) → cek masuk `cosmec` + `kendala_cosmec` lewat query DB, `view`/`list2` (badge NOK/Ada NOK bener, teks kendala kebaca), `edit_data`/`edit` (halaman kebuka), PDF export view & list2 (`%PDF-1.7` valid, generate pakai dompdf v3 yang baru di-upgrade Round 23). Regression sweep 16 halaman (semua 3 kategori: Filling 5 + Packaging 6 + Compounding 1 + infra) — semua HTTP 200. Data uji coba dibersihin lagi.
  - **Semua 3 kategori mesin (Compounding/Filling/Packaging) sekarang punya minimal 1 modul aktif** — struktur Round 6 (SIG + Menu 3-kategori) akhirnya terisi merata, bukan cuma Filling+Packaging doang.

- **Round 25: user replikasi 5 mesin Compounding lagi pakai Gemini (`DOCS_MD/PROMPT_MESIN_BARU.md`), diminta QA menyeluruh.** Mesin: **FBD Jaw Chuan** (11 part), **FBD Glatt** (11 part), **Supermixer** (7 part), **Storage Tank** (7 part), **Mixing Tank** (5 part) — semua single-machine, hidden input `mesin` (mesin id 42-46).
  - **Code review:** lint 30 file (5 controller + 25 view) — 0 syntax error. Cek pitfall yang udah didaftar di prompt: **gak ada** bug `Mysqlib`-style, `novalidate` ada di semua `add.php`, approval pakai `Menu::$approval` (gak ada hardcode Approved/Rejected/Pending), 8 method controller lengkap (termasuk `delete()`, yang paling sering kelupaan), **gak ada** collision nama fungsi `getcount_*` di `SharedController.php`, field part di controller vs kolom migration cocok semua.
  - **Catatan minor (bukan bug):** skema migration Gemini beda gaya dikit dari konvensi biasa (`DEFAULT 'OK'` di kolom part, `datetime` bukan `varchar` buat `tanggal_perubahan`/`updated_at`, `perubahan` pakai `text` bukan `varchar(255)`) — dites gak masalah secara fungsional (MySQL auto-convert), tapi beda gaya kalau nanti mau standarisasi skema.
  - **Diverifikasi end-to-end beneran buat SEMUA 5 mesin**: submit AM (1 NOK per mesin dengan kendala custom) → cek masuk tabel utama + `kendala_*` lewat query DB, `view` (badge OK/NOK jumlahnya cocok sama jumlah part per mesin, teks kendala kebaca), `list2` (badge "Ada NOK"), `edit_data`/`edit` (halaman kebuka), **submit approval beneran** (status `approval` & `user_approve` ke-update di DB), **`delete()` ditest juga** (record kehapus, DB balik 0). PDF export (`view` & `list2`) di semua 5 mesin `%PDF-1.7` valid.
  - Regression sweep 21 halaman (semua kategori: Filling 5 + Packaging 6 + Compounding 6 + Home/Approval/Users/Roles) — semua HTTP 200, nol warning/error kebocor ke halaman.
  - **Kesimpulan: replikasi Gemini buat 5 mesin ini bersih, gak ada bug yang perlu difix.** Pola dari `PROMPT_MESIN_BARU.md` efektif mencegah kelas bug yang sebelumnya ketemu di modul Best Pack (Round 22).

- **Round 26 (same-tab image lightbox + diskusi Admin CRUD (ditunda) + mulai kerjain `GAP_URS.md`):**
  - **Zoom gambar part jadi same-tab (bukan new tab lagi).** Sebelumnya klik gambar part di `add.php` buka `target="_blank"`. Investigasi awal nemu `#blueimp-gallery` container + CSS/JS-nya udah ke-load di `main_layout.php`, tapi ternyata **gak ada wiring apapun** (gak ada trigger/`data-` attribute/`blueimp.Gallery()` invocation manapun di `page-scripts.js`/`plugins-init.js`) — scaffolding mati, bukan fitur yang tinggal dipanggil. Daripada reverse-engineer API library yang gak kepake, dibikin lightbox sendiri: 1 modal Bootstrap generik (`#part-image-lightbox-modal`) + 1 click-handler jQuery (`.part-image-link`) ditambah sekali di `main_layout.php` (kepake di semua halaman), lalu 17 `add.php` (16 modul gaya baru + SIG gaya lama yang strukturnya beda) diganti dari `target="_blank"` ke `class="part-image-link"`. Diverifikasi live: 8 modul dicek via curl, `part-image-link` count sesuai jumlah part, 0 `target="_blank"` tersisa, nol error.
  - **Diskusi Admin CRUD buat konten part (Metode/Alat/Standard/Durasi/Pelaksanaan)** — user nanya pendapat sebelum eksekusi. Direkomendasikan split 2: (a) CRUD konten referensi existing part (tabel generik `part_config`, low-risk, gak nyentuh skema submission) — **tapi** ada catatan penting soal ALCOA "Original/Contemporaneous": kalau konten dibaca live dari DB, record LAMA bakal ikut nampilin teks BARU pas admin update, jadi butuh versioning/snapshot per submission biar gak jadi temuan audit; (b) nambah part/titik pemeriksaan BARU tanpa ubah kode — ini beda kelas masalah, butuh pindah ke skema EAV, refactor besar nyentuh semua 19 modul. **User memutuskan ditunda dulu** ("itu nanti dlu"), gak dieksekusi sesi ini — cuma didiskusiin.
  - **Mulai kerjain `GAP_URS.md` sesuai urutan rekomendasi yang udah ditulis di situ.** 3 item pertama (termurah) selesai:
    - **#5 Audit Trail** — tambah 1 entri `audit_log` ke `helpers/Menu.php` (fiturnya `Audit_logController.php` udah lengkap dari sebelumnya, cuma gak ke-link). Role-gating **sengaja belum** ditambahin — dicek dulu ternyata gak ada satupun menu lain yang role-gated (gak ada infra filter-per-role di `Menu.php` sama sekali), jadi nambahin gating cuma buat 1 item doang bakal inkonsisten & butuh infra baru, bukan "1 baris" lagi.
    - **#6 Printed by** — footer `report_layout.php` (shared, dipakai semua modul) diganti dari teks generik jadi `Printed by: <?php echo USER_NAME ?: '-'; ?>`.
    - **#7 Format tanggal DD/MM/YYYY** — **scope-nya ternyata lebih besar dari draft awal `GAP_URS.md`** (yang cuma based on cek SIG doang, dugaan "cuma separator beda"). Pas di-scan ke 18 modul lain, ternyata mereka **gak format tanggal sama sekali** (`echo $data['created_at']` mentah, nampilin format DB `2026-08-12 10:31:01`). Dibikin helper `format_am_date()` di `helpers/Functions.php` (`d/m/Y H:i:s`, fallback `-`), dipasang ke 48 titik (3 file × 16 modul, semua modul KECUALI SIG). **SIG sengaja dilewatin** — user minta di tengah kerjaan ("yg SIG jangan diotak-atik dlu, biarin aja") setelah sempet kepatch duluan (separator doang, langsung di-revert bersih begitu diminta, dicek `git diff` nol perubahan di `sig/view.php`/`sig/list2.php`).
    - **Bug ketauan & langsung difix pas testing (bukan lolos ke user):** script otomasi (`perl -pi -e`) buat masang `format_am_date()` ke 48 titik sempet nge-generate `format_am_date()` **tanpa argumen** di semua 48 file — root cause classic Perl gotcha: `$row`/`$data` di sisi REPLACEMENT `s///` ikut di-interpolate Perl (bukan literal), jadi `$row["created_at"]` di replacement string malah dievaluasi jadi hash kosong alih-alih ditulis apa adanya. Ini kebukti fatal live (`Too few arguments to function format_am_date()`, plus bug lama yang balik muncul: exception handler nge-render halaman error PENUH yang ke-embed mentah di dalem `<td>`, sama persis kelas bug yang udah dicatet berkali-kali sesi-sesi sebelumnya). **Ketauan sebelum lapor selesai** karena rutin live-test tiap perubahan (submit record beneran di Chimei, buka `list2`/`view`/`edit_data`) — bukan cuma `php -l` doang (lint gak nangkep undefined-function-call-arg-count). Difix pake perl escaped `\$row`/`\$data`, di-lint ulang 48 file, di-test ulang live sampe bener (`12/08/2026 10:31:01` muncul di ketiga halaman), regression sweep 16 modul HTTP 200 nol error, data test dihapus lagi dari DB.
  - `GAP_URS.md` sendiri diupdate reflect status terbaru (3 item pertama ✅, urutan sisa item digeser).
  - **Koreksi lightbox (masih di Round 26, sesi lanjutan):** user lapor modal zoom gambar "transparan" — bisa diklik tapi keliatan tembus pandang ke halaman di belakangnya (backdrop gelap Bootstrap gak render dengan bener). Daripada debug ketergantungan CSS/JS/z-index Bootstrap modal yang gak jelas konfliknya, diganti total jadi overlay custom mandiri (`#part-image-lightbox`, `position:fixed` + `background:rgba(0,0,0,.9)` + `z-index:10000`, toggle via class `.show`) — gak gantung ke Bootstrap modal plugin sama sekali, jadi imun dari konflik CSS/JS app manapun. Diverifikasi live markup & CSS-nya kekirim ke browser dengan benar (elemen + style block ada di `main_layout.php`, class `part-image-link` masih terpasang di 17 `add.php`).
  - **#1 Auto-Approve kalau semua part OK — 16/19 modul selesai.** Ditambahin logic di `add()`: cek semua `part_fields()` == `'OK'` → auto-set `approval='Approved'`, `user_approve='System'`, `tanggal_perubahan=now()`. Dikerjain via script perl (bikin & tes di 1 file dulu — Cosmec — baru di-apply ke 15 sisanya setelah kebukti bener, belajar dari insiden Perl-interpolation sebelumnya di sesi ini). **SIG dikecualikan** (masih berlaku instruksi "jangan diotak-atik dlu" dari user). Diverifikasi live di Chimei: submit all-OK → approval langsung "Approved" otomatis (dicek preselect di halaman edit); submit 1-NOK → approval tetap kosong nunggu manual. Regression sweep 16 modul + SIG (add+list2) + Home/Approval/Audit Trail/Users — semua HTTP 200 nol error. Data test dihapus lagi.
  - `GAP_URS.md` diupdate lagi reflect status #1 (16/19 selesai, SIG nunggu izin).
  - **Koreksi lightbox lagi (bug ke-2, root cause beda dari yang pertama):** user lapor gambar di lightbox blank/gak muncul (cuma backdrop gelap + tombol X doang). Ternyata bukan soal CSS overlay — pas dicek `git diff`, perl command PERTAMA yang masang `class="part-image-link"` di 16 file (sesi sebelumnya, sebelum masalah backdrop transparan) kena **persis bug interpolasi Perl yang sama** kayak insiden `format_am_date()`: replacement string `<a href="<?php print_link($image_path); ?>" ...` bikin `$image_path` ke-interpolate Perl jadi kosong, hasil akhirnya `href="<?php print_link(); ?>"` (nunjuk ke site root, bukan gambar) — SEMUA 16 file kena, SIG selamat karena pakai command terpisah yang gak nyentuh href. Bug ini gak ketauan pas verifikasi awal karena testing waktu itu cuma ngecek "target=_blank hilang, class ada" — gak sampe ngecek NILAI href-nya. **Pelajaran:** command Perl yang nulis ulang PHP variable di sisi replacement `s///` HARUS diverifikasi dengan cek isi value-nya, bukan cuma keberadaan pattern/lint doang — 2 insiden beruntun di sesi yang sama. Fix pakai `sed` biasa (bukan perl replacement kompleks) — plain string match, gak ada resiko interpolasi. Diverifikasi live: `href` di 16 modul sekarang bener-bener nunjuk ke file gambar asli, regression sweep 16 modul HTTP 200 nol broken href nol error.
  - **Perbaikan halaman Audit Trail (masih Round 26, sesi lanjutan) — user nanya 3 hal setelah nyoba fitur baru ini:**
    - **"Kok banyak banget, gak ada filter?"** — dicek `Audit_logController.php`, ternyata cuma ada free-text search doang, gak ada filter per-modul/action/tanggal. Ditambahin filter dropdown "Modul/Tabel" (populated dinamis dari `DISTINCT TableName` di DB, bukan hardcode, biar otomatis ke-update kalau ada mesin baru), "Action" (add/edit/delete/view), plus date range — pola form sama persis kayak filter di modul mesin (label + Terapkan Filter + Reset).
    - **`Deprecated: parse_str(): Passing null...` di `BaseView.php` line 468** — root cause sama kelas bug yang udah berkali-kali ketemu sesi ini (`parse_url()` balikin `null` kalau URL gak ada query string, terus fungsi berikutnya gak siap terima `null`). Fixed 1 baris: `parse_str((string) $qs, $get)`. Di-scan ulang project-wide, cuma 1 titik ini yang pakai `parse_str()`.
    - **"Kenapa ada 'add user' di log, bukannya harusnya otomatis aja?"** — dicek `write_to_log()` di `BaseController.php`: ini **emang otomatis**, bukan manual — kepicu tiap kali controller manapun (termasuk `UsersController`) manggil `$this->write_to_log('add', ...)` pas ada operasi CRUD, jadi "add user" di log itu beneran nunjukin ada admin yang nambah akun user, tercatat otomatis oleh sistem, bukan entry yang sengaja dibuat orang. **Tapi** pas ngecek ini ketemu bug beneran: halaman list-nya punya tombol **"Add New Audit Log"** yang linknya ke `audit_log/add` — padahal `Audit_logController.php` **gak punya method `add()` sama sekali**, jadi tombol itu nunjuk ke halaman "Not Found" kalau diklik. Ini juga konsepnya salah dari awal — audit trail per definisi (ALCOA, GMP) harus murni system-generated, gak boleh ada jalur buat nambah entry manual (kalau bisa, itu ngerusak integritas si audit trail-nya sendiri). **Fixed:** tombol dihapus, diganti keterangan kecil "Log ini digenerate otomatis oleh sistem... tidak bisa ditambah manual."
    - Diverifikasi live: filter jalan tanpa error, dropdown modul ke-populate bener dari data asli, tombol "Add New Audit Log" udah gak ada, `Deprecated: parse_str` udah gak muncul lagi di halaman manapun (dicek `audit_log`, `home`, `approval`).
  - **2 bug lagi ketemu di Audit Trail (masih Round 26) pas user coba-coba fitur, keduanya framework leftover dari PHPRad scaffold lama, bukan hasil kerjaan sesi ini:**
    - **Tombol "UserID" di list/view Audit Log buka modal "No record found"** — root cause: `audit_log/users-pages.php` manggil `render_page("users/list/nama/$field_value")`, tapi `$field_value` itu ID numerik user (`get_active_user('id_user')`), sementara kolom `nama` isinya nama orang (misal "Administrator") — jadi query `WHERE nama = '1'` gak akan pernah nemu apa-apa, filter field-nya salah dari awal. Fixed: ganti `nama` jadi `id_user` (field yang sesuai match sama isi `$field_value`). Diverifikasi live: modal sekarang nampilin data user asli, bukan "No record found".
    - **Kolom "Action" di halaman `view` Audit Log salah render jadi tombol biru "👁 view" yang linknya ke modal user lookup** — ternyata copy-paste error dari tombol "UserID" di baris bawahnya (href-nya pakai `id_log`, tapi teksnya nampilin `Action` — dua hal yang gak nyambung). "Action" seharusnya cuma teks polos ("add"/"edit"/"view"/"delete"), sama kayak kolom lain (Timestamp, Tablename, dst). Fixed: dibuang tombolnya, jadi teks polos. Diverifikasi live.
    - **User nyoba modal lookup yang baru difix, nanya "kenapa masih ada tombol Add New Users di situ?"** — bukan bug baru, tapi UX yang gak pas: `render_page("users/list/id_user/$field_value")` di `users-pages.php` gak nge-pass opsi `show_header => false`, jadi modal kecil yang tujuannya cuma "nunjukin siapa yang ngelakuin aksi ini" ikut nampilin header lengkap halaman Users (tombol "Add New Users", search box, breadcrumb) — padahal konteksnya cuma lookup read-only, bukan buat kelola user dari situ. Fixed: tambahin `array('show_header' => false)`, pola sama kayak yang dipake Approval buat nge-embed list mesin di dalem tab (`approval/list.php`). Tombol View/Edit/Delete di baris record-nya sendiri tetep ada (itu emang relevan, beda dari tombol "Add" di header). Diverifikasi live: "Add New Users" udah gak nongol lagi di modal ini, tabel datanya tetep kebaca normal.
  - **Diskusi role-based access (jawaban singkat sebelum lanjut):** user nanya mending coding manual atau bikin halaman checkbox super-admin buat atur akses per-role. Dicek dulu — ternyata **udah ada** tabel `role_permissions` + CRUD generic (`Role_permissionsController.php`), tapi **gak dipakai di manapun buat ngecek akses beneran** (dead infra, sama kelas kayak tombol "Add Audit Log" kemarin). Direkomendasikan: coding manual (pola `$izinKhusus`/`$admin_roles` yang udah ada) tetep cukup buat ~13 role yang jarang berubah, checkbox UI baru worth dibangun kalau ada kebutuhan konkret non-developer yang harus self-service. **User setuju ditunda, didiskusikan nanti.**
  - **Audit Trail: kurangin noise `view`/`list` (permintaan user, dieksekusi setelah dikonfirmasi maksudnya).** User ngerasa log kebanyakan isinya `view` (buka detail record doang, bukan perubahan data) dan `list` (buka halaman daftar) — nanya apa audit trail emang seharusnya cuma nyatet CRUD beneran (add/edit/edit_data/delete). Setuju secara prinsip (buat kebutuhan audit compliance, `view` gak wajib dicatat), tapi di-flag 1 hal penting: **gak boleh hapus entry `view`/`list` yang UDAH ADA di DB** — audit trail harus immutable, ngehapus history yang udah tercatat justru ngerusak inti tujuan audit trail (bisa jadi temuan lebih serius kalau diaudit). User setuju cuma stop yang baru.
    - **Fixed:** hapus baris `$this->write_to_log('view', ...)` dari **22 controller** (semua yang ada view() — termasuk `UsersController`, `RolesController`, dst, bukan cuma mesin AM) + `$this->write_to_log('list', ...)` dari 2 controller (`Illapak_1_2`, `Illapak_3_12` — cuma di situ doang yang pernah nyatet action `list`, 17 modul lain gak pernah). **`SigController.php` DIKECUALIKAN** (masih berlaku instruksi "jangan diotak-atik dlu"), `git diff` nol.
    - Dropdown filter Action (`audit_log/list.php`) diupdate: opsi `view` dihapus (gak ada gunanya lagi buat data baru), sekaligus ketauan `edit_data` kelewat gak dimasukin pas nambahin filter kemarin — ditambahin.
    - Diverifikasi live: submit + view record test di Chimei → cek audit log, entry paling atas (terbaru) `add`, BUKAN `view` (padahal langsung dibuka abis submit) — kebukti `view` beneran gak lagi ke-log. Regression sweep 16 modul mesin + SIG + `users`/`roles`/`area_id`/`line`/`tag`/`kendala`/`role_permissions`/`audit_log` — semua HTTP 200 nol error. Data test dihapus lagi.
  - **User minta hapus tombol "Add New [Mesin]" khusus di halaman Approval** ("kalau mau add ya langsung ke menu mesinnya"). Root cause: `approval/list.php` udah manggil `render_page(..., array('show_header' => false))` buat tiap mesin, tapi **16 dari 17 modul (`list2.php`) gak pernah baca/cek variable `$this->show_header` sama sekali** — tombol "Add New X"-nya hardcode selalu tampil apapun konteksnya. Cuma **SIG** yang udah bener dari awal (`if ($show_header == true) { ... tombol Add ... }`) — pola aslinya emang begitu, 16 modul hasil replikasi yang kelewat nge-drop pengecekan ini. Fixed: 16 file di-wrap tombol Add-nya pakai `<?php if ($this->show_header) { ?>...<?php } ?>`, dikerjain via script PHP kecil (bukan perl, buat hindarin jebakan interpolasi yang udah 2x kejadian sesi ini) — dites `preg_match_all` count harus persis 1 match per file sebelum replace, biar gak salah kena. **SIG gak disentuh** (kodenya emang udah bener + masih berlaku instruksi jangan diotak-atik). Diverifikasi live: halaman Approval sekarang 0 tombol "Add New" di 17 tab manapun, tapi tombol itu tetap ada & jalan normal di halaman `list2` masing-masing mesin sendiri (misal buka `chimei/list2` langsung, bukan lewat Approval). Regression sweep 17 modul (16+SIG) — semua HTTP 200 nol error.
  - **Next (waktu itu): #2/#3/#4 (lockout, session timeout, password complexity)** — semuanya sudah selesai di Round 28, lihat di bawah. Masih nunggu izin user buat nerapin #1 & #7 ke SIG.

- **Round 27: migrasi database dari MySQL/MariaDB ke PostgreSQL 17 (lokal), plus reset & aktivasi RBAC 4-role sesuai URS.** User minta cek dokumen sisa TODO, lalu (kalau gak ada) langsung migrasi ke Postgres.
  - **MySQL lokal ternyata korup** — baca metadata tabel (`SHOW FIELDS`, `information_schema.COLUMNS`) bikin `mysqld` crash berulang kali (`InnoDB: log sequence number is in the future`, ada dari 10 Agustus). Data (`SELECT` biasa) tetap aman/utuh, cuma metadata-read yang crash. Solusi: skema Postgres di-generate dari `ddl_dml.sql` + 29 file `database/migrations/*.sql` (bukan `mysqldump`), jadi gak perlu nyentuh MySQL yang rusak sama sekali. Setelah Postgres kebukti jalan, MySQL lokal dihapus (izin eksplisit user) — proses `mysqld` dimatiin, folder `data` dihapus permanen.
  - **Converter custom** (`convert.php`, bukan tool off-the-shelf) nge-generate 50 tabel Postgres (33 tabel pabrik sebelah + `illapak_1..12` legacy sengaja dilewatin, udah gak kepake kode). Ketemu & fixed 2 bug pas convert: kolom `joeya` kurang 7 (migration `ALTER TABLE` gak ke-parse di percobaan pertama — fixed, converter sekarang baca `ALTER TABLE ADD COLUMN` juga) dan kolom `pict` di `users` (MySQL `tinyblob`) ternyata isinya path file string, bukan binary — diganti `varchar` di Postgres. Cross-check independen (155 kolom part × 16 modul vs skema) 0 gap.
  - **Role direset dari 13 (campur era pabrik sebelah) jadi 4 sesuai URS 2.1**: Administrator/Manager/Supervisor/Staff-Operator. Matrix akses (URS 2.2 + 4.2) disepakati bareng user, exception: superadmin baru dibuat khusus (username `superadmin`, BUKAN NIK — pengecualian yang diminta user).
  - **RBAC infra ternyata udah ada tapi mati total** — `libs/ACL.php` (`$role_pages = '*'`, buka semua) dan `USER_ROLE` constant gak pernah didefinisikan. Diaktifkan: `USER_ROLE` (role_id int) di `config.php`, matrix `$role_pages` per role_id di `ACL.php`, dipanggil dari `SecureController::authenticate_user()` buat proteksi rute BENERAN (bukan cuma sembunyiin tombol UI) — halaman `errors/forbidden.php` yang udah ada sebelumnya dipakai. Juga fix 18 file `view.php`/`sig/list2.php` yang **hardcode role_id lama** (`25,13,17,26` dst) — kalau gak dibenerin, tombol approve/edit/delete bakal patah total karena role_id itu udah gak ada di skema baru. **SIG kena fix ini juga** (cuma angka role_id, bukan behavior/format) — dianggap perlu karena ini regresi langsung dari migrasi role, bukan feature change yang kena larangan "jangan diotak-atik".
  - Diverifikasi live pakai 4 akun beda role: Operator/Manager kena 403 beneran (bukan cuma disembunyiin) pas nembak `edit_data`/`delete`/`editfield`/`add` langsung lewat URL sesuai role masing-masing; menu sidebar ikut ke-filter otomatis; superadmin tetap bebas semua. `http_response_code()` yang gak pernah dipanggil buat halaman 403/404 juga dibenerin (sebelumnya nampilin body "Forbidden" tapi status HTTP tetep 200).

- **Round 28: audit QA & production-readiness menyeluruh (5 pilar) atas permintaan user, semua non-SIG. Ketemu 1 CRITICAL security beneran + 3 fitur URS (#2/#3/#4) dikerjain sekalian.**
  - **CRITICAL kebukti live (bukan dugaan baca kode)**: `ApiController::json($action,...)` manggil method PUBLIC APAPUN di `SharedController` (termasuk yang diwarisi dari `BaseController`, misal `write_to_log()`) berdasarkan `$action` dari URL, **tanpa login dan tanpa whitelist**. Dibuktikan: `curl .../api/json/write_to_log/HACKED_BY_AUDIT/fake` beneran nyuntik entri palsu ke `audit_log` tanpa autentikasi. **Fixed**: whitelist 8 method yang beneran dipakai UI. Bonus temuan pas bikin whitelist: halaman **My Account** patah (`account_username_value_exist`/`account_user_role_id_option_list` gak pernah ada di `SharedController` — Error 500 tiap buka Account) — fixed dengan ngarahin view ke fungsi `users_*` yang udah ada & fungsinya sama.
  - **`DEVELOPMENT_MODE` hardcode `true`** di `config.php`, gak baca `.env` kayak constant lain — full path server + stack trace bocor ke response TANPA LOGIN (dibuktikan pas nguji temuan di atas). Fixed jadi env-driven, `.env`/`.env.example` di-set `false`.
  - **`MasterdetailController`** masukin `$master_page`/`$detail_page` mentah ke `include()` tanpa whitelist (potensi LFI terbatas, cuma bisa target file `*-pages.php`). Sempat dikira bisa bypass RBAC liat data Users sebagai Operator — **ternyata SALAH setelah dites langsung**, controller tujuan tetep ngecek ACL sendiri pas di-instansiasi ulang. Tetap dikasih whitelist buat hardening (cuma `audit_log/users` yang diizinin, satu-satunya file `*-pages.php` yang ada).
  - **Dead code dibersihin** (izin eksplisit user): `Form_taggingController.php` + view-nya (nunjuk tabel yang gak pernah ada), dan ~40 fungsi mati di `SharedController.php` (857→248 baris, semua nunjuk tabel pabrik sebelah yang udah gak ada di Postgres). **Insiden pas ngerjain**: edit pertama gak sengaja ikut ngehapus 11 fungsi `getcount_*` yang MASIH AKTIF dipakai `home/index.php` — ketauan sebelum lapor selesai (cross-check daftar fungsi yang dibutuhkan vs yang tersisa, bukan cuma percaya lint), langsung direstore dari isi yang sempat kebaca sebelum edit, diverifikasi Home dashboard live nol error.
  - **35.7MB file backup/dump dihapus** (izin eksplisit user, sempat diblokir auto-mode classifier buat operasi gabungan — lolos kalau dipecah per-file): `ddl_dml.sql`, `form_am_plg.sql`, folder `_backup_pabrik_sebelah_removed_2026-08-07/`, `.env.bak-mysql`, `php.ini.bak-preposgres`, `mysql/data_old`, `mysql/data_DELETED_20260813`.
  - **#2 Lockout, #3 Session timeout, #4 Password/NIK** — semua dikerjain & diverifikasi (jawaban keputusan sudah didapat user sebelumnya di sesi yang sama). Detail lengkap di `DOCS_MD/GAP_URS.md`. Poin penting: #3 pakai idle-timer JS di `main_layout.php` (shared ke SEMUA halaman termasuk SIG tanpa nyentuh file SIG) + auto-save draft form ke `localStorage` (selector generik `form[novalidate]`, konsisten di 17 form) + server-side backstop di `SecureController`. **Bug ketemu pas ngerjain**: `set_page_error()` di `BaseController.php` manggil `$this->db->getLastError()` tanpa cek null (dormant lama, baru ketrigger sama code path timeout baru) — fixed 1 baris guard.
  - **Verifikasi #3 pakai browser beneran** (bukan cuma curl, karena JS gak bisa dites via curl) — Edge headless + Chrome DevTools Protocol (gak ada network buat install Playwright, `chromium-cli` juga gak ada), screenshot asli: modal peringatan render rapi, isi form → draft ke-save 47 field ke localStorage → reload → notice restore → klik restore → nilai balik persis → localStorage bersih. Nol console error di semua langkah.
  - Regression sweep akhir: 17 modul mesin (list2+add) + 8 halaman infra, semua HTTP 200 nol warning/error kebocor.

- **Round 29: SIG akhirnya boleh disentuh** ("gas aja deh gpp rubah SIG dsb") — #1 auto-approve dan #7 format tanggal diterapkan ke SIG, modul terakhir yang belum kena. Sekaligus ketemu **3 bug SIG-Postgres yang gak kebukti di Round 27** (waktu itu cuma dites `view`/`list2` SIG, gak pernah dites submit form beneran lewat controller):
  1. **`SigController::add()` gagal total di Postgres** (`SQLSTATE[42703]: column "Mesin" does not exist`) — SIG pakai nama field capitalized (`Mesin`, `Sealing_Cross_dan_Vertikal`, dst, warisan MySQL yang case-insensitive buat nama kolom), sementara `PDODb::insert()` selalu nge-quote nama kolom dari key array (case-sensitive beneran di Postgres). **Sempat salah fix duluan**: coba rename kolom Postgres ke capitalized (biar match kode) — ternyata SALAH ARAH, karena `SELECT`/`JOIN` di PDODb itu **raw string gak di-quote** (Postgres otomatis lowercase apapun casingnya di situ), cuma `INSERT`/`UPDATE` yang di-quote persis sesuai key. Direvert balik ke lowercase (konsisten sama 16 modul lain), fix yang bener: remap `$modeldata` (capitalized) ke array lowercase (`$db_row`) tepat sebelum `$db->insert()`.
  2. `edit_data()` ada bug turunan dari akar yang sama: `$db->getOne($tablename, "Mesin")` lalu baca `$sig_data['Mesin']` — SELECT field yang gak di-quote balik jadi lowercase di hasil query, jadi key-nya `mesin` bukan `Mesin`. Fixed disamain lowercase di kedua sisi.
  3. **Typo lama** (bukan dari migrasi Postgres, dari awal SIG dibikin): `add()`'s `$daftar_bagian` nulis `"Rol_Penarik_Sachet_dan_Slitting_Shim"` (kurang kata `_Foil_`) buat lookup `$_POST["Kendala_..."]`, padahal field aslinya ada `_Foil_`-nya — akibatnya kendala teks buat part itu **selalu ke-drop diam-diam**, gak pernah kesimpen ke `kendala_sig`. Fixed jadi sama persis nama field aslinya.
  - Diverifikasi live end-to-end: submit all-OK (auto-approve jalan), submit 1-NOK khusus di part yang kena bug typo (kendala kesimpen bener), `view()`, `edit_data()` GET+POST (prefill + resync kendala), `list2()`, `edit()` approval, PDF export, `delete()` — semua lewat curl, nol error. **Semua 19 modul (termasuk SIG) sekarang konsisten** buat #1 dan #7 — detail lengkap di `GAP_URS.md`.
  - **Kesimpulan penting buat sesi depan**: kalau ada modul lain yang nanti pindah/ganti DB engine, JANGAN asumsikan "kalau `view`/`list` udah kebukti jalan berarti semua CRUD aman" — read-path (SELECT) dan write-path (INSERT/UPDATE) di `PDODb` punya perilaku quoting yang beda soal case-sensitivity, jadi keduanya harus dites terpisah, bukan diekstrapolasi dari salah satu doang.

- **Round 30: "Joeya-kan SIG" — langkah 1 dari rencana `BaseMachineController`, akhirnya dieksekusi.** User izinin lanjut ("mulai sekarang, langkah 1 dulu") setelah nanya soal efisiensi controller.
  - `SigController.php` ditulis ulang total dari 816 baris (hardcode per-part, style PHPRad lama) jadi **204 baris** pola `$parts` + `foreach`, sama persis kayak 16 modul lain (Joeya dipakai sebagai template acuan). 5 view file (`add.php`, `view.php`, `edit.php`, `edit_data.php`, `list2.php`) ditulis ulang total juga biar field naming konsisten lowercase di semua tempat (nutup celah case-sensitivity yang bikin bug Round 29). `sig/list.php` (view lama buat `index()`, udah gak kepake karena `index()` sekarang delegasi ke `list2()` kayak Joeya) dihapus — dicek dulu nol referensi ke file itu di seluruh app sebelum dihapus.
  - **2 kekhususan SIG yang harus dipertahankan** (beda dari 16 modul lain, ketauan dari baca `sig/add.php` lama sebelum ditulis ulang, bukan ditebak): part `antistatic` pakai 3 pilihan radio (`Menu::$antistatic` — OK/NOK/N-A), bukan 2 pilihan (`Menu::$Kondisi_Harian`) kayak part lain; part `tekanan_angin_suplai` punya 1 input angka tambahan (`value_tekanan_angin`, satuan BAR) nempel di radio-nya. Ditangani pakai percabangan kecil di view (`if ($field === 'antistatic') ...`) tanpa ngerusak pola generik `$parts`-nya.
  - **Data metode/alat/standard/durasi/pelaksanaan 11 part diekstrak dari HTML lama** (bukan ditulis ulang dari ingatan) pakai kombinasi regex + baca manual per-section (regex pertama sempet ke-geser karena HTML `Antistatic` beda struktur dari part lain). User juga sempet share screenshot Excel buat cross-check — cocok persis sama data yang udah diekstrak.
  - **Diverifikasi live end-to-end** (`DEVELOPMENT_MODE` diaktifkan sementara buat lihat error asli, dimatikan lagi setelahnya): submit all-OK (auto-approve `Approved`/`System` jalan), submit 1-NOK di part `antistatic` (kendala kesimpen ke `kendala_sig`, badge merah tampil di `view`/`list2`), `edit_data()` GET+POST termasuk ganti value ke opsi ke-3 `N/A` (badge abu-abu tampil bener, kendala ke-clear otomatis karena N/A bukan NOK), `edit()` approval, PDF export (`%PDF-1.7`), `delete()` — semua lewat curl, nol error. Regression sweep 40 halaman (17 modul mesin × 2 + 6 infra) — semua HTTP 200.
  - **Insiden kecil pas testing**: submit pertama pakai `value_tekanan_angin=1.2` (desimal) gagal karena kolom DB-nya `integer`, bukan bug kode — cuma salah data test, langsung diulang pakai angka bulat.
  - **Langkah 2 (`BaseMachineController` beneran, 17 controller jadi ~5 baris masing-masing) belum dikerjain** — nunggu sesi terpisah kalau user mau lanjut, estimasi ½-1 hari.
  - **Ketemu gak sengaja (di luar scope refactor, dilaporkan ke user, BUKAN dieksekusi tanpa izin)**: folder `assets/images/sig/` ternyata **emang gak pernah ada dari awal** — dicek ke git history (cuma 1 commit total di repo ini), gak pernah ke-commit. Bukan kehapus sesi ini (dicross-check ke semua command `rm` yang dijalanin hari ini, nol yang nyentuh `assets/images/`). User bakal upload manual belakangan.

- **Round 31: 2 bug ketemu pas user testing manual pakai 4 akun role baru.**
  1. **Audit Trail Error 500 buat SEMUA user** (termasuk superadmin) — `Audit_logController.php` masih pakai nama kolom `audit_log` yang capitalized (`Timestamp`, `Action`, `TableName`, `UserID`, dst — peninggalan MySQL) tapi gak di-quote di raw SELECT/WHERE, jadi Postgres otomatis lowercase-in dan gagal match kolom asli (`Timestamp` bahkan reserved word di Postgres). **Persis kelas bug yang sama kayak SIG di Round 29**, cuma di controller lain. Fixed: semua referensi kolom di-quote (`"Timestamp"`, dst) di `$fields` array, search condition, `$db->where()`, dan `rawQuery`. Ketemu 1 bug lagi pas testing search: `audit_log.log_id LIKE ?` gagal karena `log_id` integer dan Postgres gak izinin `LIKE` ke integer tanpa cast eksplisit (beda dari MySQL yang izinin implicit) — fixed pakai `CAST(... AS VARCHAR)`. Diverifikasi live: index, search, filter action/table/tanggal, kombinasi filter, modal lookup UserID, semua HTTP 200 nol error.
  2. **Tombol "Approval" muncul ke SEMUA role di `list2.php`, gak di-role-gate** — user nemuin ini pas coba akun Operator: tombol Approval keliatan & bisa diklik, tapi backend nolak (403) karena ACL emang gak ngizinin. Beda dari `view.php` yang emang udah bener (ada `$can_approve` check dari awal), `list2.php` (kolom Aksi per-baris) gak pernah dikasih pengecekan yang sama sejak awal dibikin — bug **di SEMUA 17 modul** (16 modul + SIG hasil Round 30), bukan cuma 1. User nanya prinsipnya: "kalau emang gak ada authorization, mendingan tombolnya jangan ditampilin dari awal" — disetujui, itu emang UX yang lebih benar (jangan kasih tombol yang keliatan hidup tapi ternyata mati). Fixed: tambah `$can_approve` (pola sama persis kayak `view.php` — `$izinKhusus=[2]` Manager, `$admin_roles=[1,3]` Administrator+Supervisor) di header tiap `list2.php`, tombol Approval di-wrap kondisional. Dikerjain via script PHP `str_replace` literal (bukan regex sed/perl, ngehindarin jebakan yang udah 2x kejadian sesi ini), tiap match count divalidasi = 1 sebelum di-replace. Diverifikasi live: Operator gak lagi liat tombol Approval di manapun; Administrator/Manager/Supervisor tetep liat & bisa pakai. Regression sweep 17 modul + halaman Approval — semua HTTP 200.
  - **Prinsip buat ke depan** (dicatat biar konsisten): backend authorization (ACL role-gating di `SecureController`) itu **lapisan wajib** yang udah solid dari Round 27-28 — itu yang nyegah data leak/unauthorized action beneran. Tapi **UI harus juga nyembunyiin aksi yang bakal ditolak**, bukan cuma ngandelin backend buat nolak pas diklik — kalau enggak, user ngerasa "kok tombolnya ada tapi error" (persis keluhan yang baru ketemu). Kalau nambah controller/view baru ke depan, cek KEDUA elemen tindakan (row-action di list DAN detail-page button di view) pakai `$can_approve`/`$can_edit`/`$can_delete` yang konsisten, jangan cuma salah satunya.

- **Round 32: user ngerasa "password minimal masih 6" pas coba register.** Backend-nya sendiri sebenarnya udah bener (dicek live: password 6 karakter ditolak beneran, akun gak kebuat) — masalahnya di **UI kasih sinyal yang salah**. Plugin JS `passwordStrength` (`assets/js/plugins.js`) punya default `minChars: 6` sendiri (dipilih dari `input.attr('minlength')`, fallback ke 6 kalau atribut itu gak ada) — karena field password di `register.php`/`users/add.php` gak punya `minlength` sama sekali, indikator "panjang terpenuhi" nyala ijo pas user baru ngetik 6 karakter, padahal submit-nya bakal ditolak server di 8. Ditambah, teks hint di UI eksplisit nulis **"6 Characters minimum"** (hardcoded, gak ke-update dari Round 26 pas rule 8-karakter ditambahin) dan gak nyebut syarat huruf kecil sama sekali padahal wajib. Fixed di `register.php` + `users/add.php`: attribute `minlength="8"` ditambahin (bikin plugin JS otomatis pakai 8, bukan fallback 6), teks hint diganti "8 Characters minimum", ditambahin chip "Lowercase Letter" yang sebelumnya gak ada. `passwordmanager/password_reset_form.php` (reset password) sebelumnya malah gak pakai plugin sama sekali (input polos, nol hint) — ditambahin `minlength="8"` + teks hint manual.
  - **User juga nanya soal NIK**: dikonfirmasi kode udah bener sejak Round 26 (username akun baru wajib NIK, dicek live sebelumnya), tapi **form-nya juga gak ngasih tau user aturan ini sama sekali** (placeholder generik "Enter Username", nol hint) — kelas masalah yang sama kayak password di atas: rule-nya jalan di backend tapi gak dikomunikasikan di UI, jadi user baru tau pas ditolak submit. Fixed: placeholder diganti "NIK (huruf & angka, 8 karakter)" + `minlength`/`maxlength="8"` + hint teks di `register.php` dan `users/add.php`.
  - **Diverifikasi live**: buka kedua halaman, cek `minlength="8"` dan teks hint baru beneran ke-render di HTML (curl, bukan cuma baca kode). Regression check `home`/`account/edit` tetap 200.

- **Round 33: RBAC "own record" gap dikonfirmasi & difix, laporan URS-compliance dibikin, lalu `BaseMachineController` (Langkah 2 dari rencana Round 30) akhirnya dieksekusi — SEMUA 17 modul mesin sekarang pakai base class, bukan cuma SIG.**
  - **RBAC gap (URS 3.1):** dicek ulang matrix akses per role, ketemu 2 gap vs URS: Operator gak bisa `edit_data` submission sendiri, Manager gak punya akses `delete` padahal URS ngizinin. User konfirmasi 3 keputusan: fix Operator edit_data (ya), kasih Manager delete (ya), Supervisor edit_data juga dibatasi ke record sendiri (pilih yang lebih ketat, bukan default "full akses" dari Round 27). "Own record only" gak bisa diekspresiin di `ACL.php` (cuma tau actions, bukan record ownership) — diimplementasi sebagai pengecekan `user_create === USER_NAME` di awal tiap method `edit_data()` per controller (Administrator role 1 bebas). **Insiden kecil pas testing**: sempet lupa nambahin `'edit_data'` ke role 4 (Staff/Operator) di `ACL.php` — cuma ke role 2 (Manager) — ketauan lewat curl test Operator dapet 403 di record sendiri, di-debug pakai script isolasi `getOne()` (kebukti query-nya sendiri bener) baru ketauan akar masalahnya di layer ACL bukan controller. Fixed, diverifikasi ulang 4 skenario (Operator-own=200, Operator-lain=403, Manager-lain=403, Manager-delete+CSRF=302 sukses).
  - **Laporan checklist kepatuhan URS** dibikin dalam 2 bentuk: artifact HTML (`https://claude.ai/code/artifact/4d7316f1-f2da-4206-b393-bc29eeb4c5f1`, tema light/dark) dan `DOCS_MD/CHECKLIST_KEPATUHAN_URS.md` — nyakup Registrasi Akun, Login & Sesi, Role & Kewenangan (termasuk matrix di atas), Approval, Report/Export, ALCOA, dan item di luar ranah kode.
  - **`BaseMachineController` (`system/BaseMachineController.php`, ~260 baris) akhirnya dibikin** — abstract class extend `SecureController`, isinya semua 8 method generik (index/list2/add/view/edit/edit_data/editfield/delete) yang sebelumnya diduplikasi identik di 17 controller. Subclass tinggal declare 3 property: `$machineKey` (nama tabel), `$displayName` (judul halaman/laporan), `$parts` (field=>label). `$extraFields` jadi escape hatch buat field tambahan di luar part OK/NOK (dipakai SIG buat `value_tekanan_angin`). Di-require manual di `index.php` (bukan lewat `autoloadController()` yang cuma nyari di `app/controllers/`, karena base class ini taruh di `system/`).
  - **Migrasi dikerjain bertahap, divalidasi tiap tahap**: pilot 1 controller (Chimei, paling sederhana) → tes end-to-end penuh (submit NOK, view, edit_data, PDF, delete, RBAC ownership) → baru batch 15 controller sisanya (non-SIG) pakai script ekstraksi otomatis (`$parts` array diekstrak dari file lama via regex, di-lint tiap file sebelum dianggap sukses) → SIG dikerjain terakhir & terpisah (pakai `$extraFields` buat `value_tekanan_angin`; 3-opsi radio Antistatic tetap gak disentuh karena itu murni logic di view, bukan controller). Tiap controller susut dari ~200 baris jadi 15-25 baris.
  - **Diverifikasi live end-to-end** buat kasus paling berisiko (modul multi-mesin dengan dropdown, bukan hidden input): submit Illapak 1-2 (1 NOK) → view/list2 (badge NOK bener)/edit_data (prefill bener)/PDF (`%PDF-1.7` valid)/delete — semua sukses. SIG (kasus paling khusus): submit dengan antistatic NOK + `value_tekanan_angin` → view nampilin "(5 BAR)" bener, kendala tersimpan, PDF valid, delete sukses. Final regression sweep: 17 modul (list2+add) + Home/Approval/Users/Roles/Audit Trail — semua HTTP 200 nol error/warning/deprecated.
  - **2 folder backup lama dihapus** (`_backup_race_condition_2026-08-07` dan `_backup_race_condition_2026-08-07_pass2`, izin eksplisit user).
  - **Misteri "gambar SIG masih muncul di web"** diinvestigasi — dikonfirmasi via git history (cuma 1 commit total di repo) dan cross-check semua command hapus sesi ini: folder `assets/images/sig/` **emang gak pernah ada dari awal**, bukan kehapus. URL gambar yang gak ada balik HTTP 200 tapi `Content-Type: text/html` (halaman 404 router, gara-gara `.htaccess` `RewriteCond !-f` nge-route semua file yang gak eksis ke `index.php`). Kemungkinan besar itu cache browser dari sesi lama — disaranin hard refresh (Ctrl+Shift+R) buat konfirmasi.
  - `DEVELOPMENT_MODE` di `.env` dibalikin ke `false` di akhir sesi.
  - **BELUM SEMPAT sesi ini** (sesi diputus karena limit, lanjut sesi depan): apply Apache restart biar `.env` kebaca ulang bersih (biasanya kebaca on-the-fly tapi belum diverifikasi eksplisit), belum update `DOCS_MD/FINAL_IMPROVEMENT.md` dengan status refactor selesai. Kode + testing semuanya udah kelar & diverifikasi live, cuma dokumentasi tambahan yang ketinggalan.

- **Round 34 (code review `BaseMachineController.php` atas permintaan user, 2 temuan nyata difix):**
  - **Stored XSS — teks kendala di-echo tanpa escaping (RIIL, dibuktikan live, bukan teoretis).** Teks kendala diambil dari `$_POST['kendala_'.$field]` (gak lewat `sanitize_array`, karena field kendala emang gak pernah masuk situ dari awal) lalu di-`echo` mentah di `view.php` (14 dari 17 modul — 3 modul: Illapak 1-2, Illapak 3-12, Jinsung 1-4 ternyata **udah bener** dari awal, konsisten pakai `htmlspecialchars()`) dan di-prefill mentah ke `<textarea>` di `edit_data.php` (semua 17). Beda dari kategori "security" lain yang di-skip di `FINAL_IMPROVEMENT.md` (asumsinya penyerang butuh akses jaringan kantor dulu) — di sini **pelakunya user internal yang emang udah legit login** (Staff/Operator sekalipun bisa nyuntik lewat form isian normal). Fixed: `htmlspecialchars()` ditambahin di 14 `view.php` + 17 `edit_data.php` (31 titik total), dikerjain via script PHP `str_replace` literal + verifikasi count sebelum replace (bukan regex). **Diverifikasi live beneran** (bukan cuma baca kode): submit Chimei dengan payload `<script>alert('xss')</script> & test "quotes"` di kendala → cek `view`/`edit_data` nampilin `&lt;script&gt;` (ke-escape, bukan ke-eksekusi), PDF export tetep valid, data test dihapus lagi.
  - **Insert kendala gak transactional** — loop insert ke `kendala_*` di `add()`/`edit_data()` jalan 1-per-1 tanpa `BEGIN/COMMIT`, kalau ada 1 insert gagal di tengah (misal koneksi putus), record utama kesimpen tapi kendala kelewat sebagian = data setengah-matang. Berlaku ke 17 modul sekaligus karena logic-nya udah disatukan. Fixed: dibungkus `$db->startTransaction()`/`commit()`/`rollback()` (PDODb udah support, tinggal pakai) di kedua method. **Diverifikasi live**: submit normal (all-OK & 1-NOK) tetap sukses persis kayak sebelumnya, regression sweep 17 modul (list2+add) — semua HTTP 200 nol error.
  - **1 temuan lain dicatat tapi SENGAJA gak difix** (prioritas rendah, bukan dead-end): `$fieldname` di `index($fieldname, $fieldvalue)` masuk `$db->where()` mentah jadi nama kolom SQL — tapi gak ketemu pemanggil yang expose `$fieldname` dari input user-controlled (URL), jadi risikonya teoretis buat sekarang. Dicatat buat kalau nanti ada fitur baru yang nge-expose parameter ini ke URL.
  - Sekalian dikonfirmasi ke user: aplikasi ini **PHP Native murni, bukan Laravel** — `composer.json` cuma punya 1 dependency (`dompdf/dompdf`), router/ORM (`PDODb`)/autoloader semuanya custom/PHPRad-generated.

- **Round 35 (bebersih dead code/file, ditemukan lewat scan sistematis + verifikasi grep per-item, semua dieksekusi atas konfirmasi user):**
  - **Dihapus langsung (yakin/konfirmasi user):** `SharedController.php.bak` (backup 1191 baris, versi aktif sekarang 248 baris), 1 file scratchpad sesi Claude yang nyasar ke root project, `generate_machines.php` (script generator sekali-pakai, sudah superseded), entry `'form_tagging' => '*'` di `ACL.php` (nunjuk controller yang udah gak ada), 12 file migration `create_illapak_1.sql` s/d `create_illapak_12.sql` (tabel standalone-nya **gak pernah dibuat** di Postgres — cross-check `\dt` cuma ada `illapak_1_2`/`illapak_3_12`, jadi murni artefak historis).
  - **Dicek dulu sebelum hapus (penting, jangan diulang kalau nemu pola serupa)**: awalnya khawatir 12 baris mesin "Illapak 1" s/d "Illapak 12" di `insert_machines.sql` juga dead — **ternyata SALAH**, itu justru mesin id 5-16 yang **beneran dipakai live** sebagai pilihan dropdown di `Illapak_1_2Controller`/`Illapak_3_12Controller` (dikonfirmasi query `SELECT id,nama_mesin FROM mesin WHERE nama_mesin ILIKE '%illapak%'` cocok persis sama dropdown yang di-render). **Baris mesin ini TIDAK dihapus.** Pelajaran: "tabel standalone gak dipakai" ≠ "baris data terkait juga gak dipakai" — cek keduanya terpisah.
  - **4 controller CRUD master-data dihapus** (controller + view folder + entry `ACL.php`, tabel di DB **sengaja dibiarkan**, bukan di-drop): `Role_permissionsController` (infra mati dari Round 26, `ACL.php` gak pernah baca tabelnya), `Area_idController` (0 baris data, field "Area" di form registrasi itu teks bebas bukan dropdown dari tabel ini), `LineController` (39 baris data tapi 0 query JOIN/SELECT ke tabel `line` di luar controller-nya sendiri), `KendalaController` (tabel generik `id_kendala`/`action`/`status`/`user_followup` — beda total dari `kendala_sig`/`kendala_chimei` dst yang dipakai form AM, sisa fitur follow-up tracking dari sistem lama pra-Round 6).
  - **2 controller serupa DIPERTAHANKAN** setelah dicek ternyata legit: `RolesController` (tabel `roles` dipakai buat assign role user di `users/add.php`) dan `TagController` (tabel `tag` dipakai buat dropdown "Kategori Tag" di **semua** form AM lewat `SharedController::sig_kategori_tag_option_list()`). Keduanya gak ada link sidebar tapi itu wajar — pola "hidden admin page" yang diakses langsung via URL, sesuai URS 1.5 soal Administrator kelola data master.
  - **Diverifikasi live**: 4 controller yang dihapus balik "Not Found" bersih (gak ada Deprecated/Fatal leak), regression sweep infra (`Home`/`approval`/`users`/`roles`/`tag`/`audit_log`) + 2 modul mesin — semua HTTP 200 nol error.
  - **Temuan besar yang di-flag ke user & dikonfirmasi**: production server (`10.167.170.71`) **belum dimigrasi** — masih MySQL lama, semua kerjaan sejak Round 27 baru ada di Postgres lokal. Rencana cutover ditulis di `DOCS_MD/DEPLOYMENT_PLAN.md` (belum dieksekusi, nunggu window & keputusan bisnis mapping role/data).

- **Round 36 (setup testing otomatis + manual checklist, atas permintaan user "bikin manual & automation testing"):**
  - **PHPUnit 10 + Guzzle** di-setup sebagai dev-dependency (`composer require --dev`). **Sempat 2x kejadian keblokir jaringan GitHub pas instalasi** (persis pola Round 14/23 — `packagist.org` bisa diakses tapi `codeload.github.com`/`api.github.com`/`github.com` timeout total), user ganti wifi di tengah sesi dan jaringan pulih, instalasi lanjut sukses. Sempat juga kena 1 gangguan lokal gak terkait jaringan (7-Zip extraction gagal buat 2 package kecil, `phar-io/version`+`phar-io/manifest` — cuma retry `composer install` biasa dan langsung sukses, bukan masalah jaringan).
  - **Pendekatan test: integration test level HTTP** (pakai Guzzle, bukan unit test PHPUnit murni) — karena app ini PHP Native tanpa dependency injection, controller gak bisa di-instantiate terisolasi buat unit test beneran (butuh session/DB/konstanta real). Jadi test-nya niru pola yang saya pakai manual via curl sepanjang sesi refactor `BaseMachineController`: login beneran, ambil CSRF token, submit form, cek efeknya — bedanya sekarang otomatis & bisa diulang. Alasan lengkap + kenapa gak pakai Playwright (dicoba dijelasin ke user: PHPUnit buat backend/RBAC/DB, Playwright biar dipakai kalo mau nutup skenario JS kayak session-timeout/draft-autosave yang PHPUnit gak bisa jangkau — belum di-setup sesi ini) ada di `DOCS_MD/AUTOMATED_TESTING.md`.
  - **31 test ditulis, 5 file** (`tests/Feature/`): `AuthTest` (login 4 role + gagal login — SENGAJA gak ada test lockout 3x karena bakal ngunci akun dummy yang dipake test lain), `SmokeTest` (list2+add semua 17 modul + 7 halaman infra, cek 200 + nol error marker — otomasi dari regression sweep manual yang saya ulang-ulang sepanjang sesi), `MachineCrudTest` (full lifecycle submit→view→edit_data→PDF→delete buat 3 modul representatif: Chimei/single-mesin, Illapak 1-2/dropdown-mesin, SIG/`$extraFields`), `RbacTest` (regresi guard buat gap URS 3.1 Round 33 — Operator cuma edit_data punya sendiri, Manager gak bisa add tapi bisa delete), `XssEscapingTest` (regresi guard buat stored-XSS fix Round 34). 2 helper class di `tests/Support/`: `ApiClient` (wrapper Guzzle+cookie+CSRF) dan `FormScraper` (parse HTML add.php buat dapetin daftar field part & mesin — test GAK hardcode field per modul, jadi otomatis nyesuain kalau `$parts` berubah).
  - **Semua 31 test lolos** (374 assertion, ~46 detik) pas dijalanin pertama kali. Diverifikasi juga DB balik bersih abis test run (`tearDown()`/`finally` di tiap test yang bikin data beneran ngehapus lagi) — 1 record SIG nyasar yang ketemu pas cek ternyata leftover dari testing manual EARLIER di sesi ini (bukan dari PHPUnit), udah dibersihin juga.
  - **`DOCS_MD/MANUAL_TESTING_CHECKLIST.md` dibikin** — checklist 8 section (Login/Registrasi, Session & Idle Timeout, RBAC per role, CRUD, Export, Audit Trail, Approval Page, Regresi Cepat) buat skenario yang gak diotomasi (terutama JS/browser: idle timer, draft auto-save, lockout).
  - Cara jalanin: `vendor/bin/phpunit --testdox` (butuh Apache + Postgres nyala duluan, app-nya bukan di-mock). Detail lengkap di `DOCS_MD/AUTOMATED_TESTING.md`.

- **Round 37 (perluas testing otomatis + Playwright, ketemu 3 bug produksi nyata pas nulis test-nya):**
  - **6 test PHPUnit baru ditambahin** (`ExportFormatsTest`, `ApprovalFlowTest`, `AuditTrailTest`, `RegistrationTest`, `LockoutTest` — 7 test method total) buat nutup gap yang diidentifikasi sesi sebelumnya. `LockoutTest` **beneran ngetest lockout 3x** pakai akun throwaway yang dibikin & dihapus sendiri tiap run (bukan salah satu dari 4 akun dummy), biar gak ganggu test lain.
  - **Bug #1 (kritis, ketemu duluan sebelum sempat nulis test export)**: CSV & Excel export **CRASH TOTAL** (Fatal error `array_keys(): Argument must be of type array, TypeError given`) kalau list mesin lagi **KOSONG** (0 record) — `current(array())` balikin `false`, bukan array, dan `array_keys(false)` meledak di PHP 8. **Ke-cascade lebih parah**: `exception_handler()` di `index.php` bikin `BaseView` baru yang MASIH baca `?format=csv/excel` dari request yang crash, jadi nyoba nge-export ulang objek exception-nya sendiri (bug arsitektur yang sama persis kayak Round 14 buat PDF, ternyata belum pernah ditutup buat format lain). **3 fix**: (a) `arr_to_csv()` di `helpers/Functions.php` — guard `current($data)` sebelum `array_keys()`; (b) export Excel di `system/BaseView.php` — guard sama + `XLSXWriter` butuh minimal 1 `writeSheetHeader()` call biar gak nolak nulis file ("no worksheets defined") walau 0 data; (c) **fix akar**: `exception_handler()` sekarang paksa `$view->format = 'html'` sebelum render, jadi halaman error SELALU html apapun format request aslinya — nutup KELAS bug ini buat semua format (pdf/word/csv/excel/json) sekaligus, bukan cuma tempel di 1 titik. **Bonus ketemu**: extension PHP `zip` ternyata gak aktif dari awal (`php.ini` XAMPP, `;extension=zip` dikomentari) — Excel export gak akan pernah jalan sama sekali walau sudah dikasih data, diaktifkan + Apache di-restart.
  - **Bug #2**: halaman **search Users, Roles, Tag, Approval CRASH (Error 500)** — `id_user`/`user_role_id`/`role_id`/`tag.id`/`approval.id` semuanya kolom integer di-`LIKE`-kan langsung tanpa `CAST`, persis kelas bug yang sama kayak `audit_log.log_id` di Round 31 tapi belum pernah di-scan ke controller lain. Fixed 4 file (`UsersController`, `RolesController`, `TagController`, `ApprovalController`) — `CAST(... AS VARCHAR)`. Ketauan gak sengaja pas `LockoutTest` butuh nyari akun throwaway lewat search buat di-cleanup.
  - **Bug #3 (ketemu pas nulis Playwright test buat session-timeout)**: auto-logout pas idle timeout **KELIHATAN jalan (redirect) tapi SESSION-NYA GAK BENERAN MATI** — JS `doTimeout()` di `main_layout.php` manggil `index/logout` **tanpa `csrf_token`**, padahal `logout()` di `IndexController` wajib `Csrf::cross_check()` yang bakal `render_error(403); exit;` SEBELUM `session_destroy()` sempet jalan kalau token gak ada/salah. Jadi timeout URS 1.3 selama ini gak beneran nutup sesi user yang idle — cuma nampilin halaman gak jelas ("Cross-Site Request Forgery Detected") sementara session tetap aktif di background. **Fix**: `LOGOUT_URL` JS var baru di `main_layout.php`, di-render PHP-side pakai `Csrf::$token` (pola sama kayak link Logout manual di `appheader.php`), `doTimeout()` sekarang pakai variable itu bukan URL hardcode. **Diverifikasi Playwright beneran** — sebelum fix, test macet nunggu form login yang gak pernah muncul; setelah fix, lolos bersih.
  - **`SESSION_TIMEOUT_SECONDS` dibikin overridable lewat `.env`** (`config.php`, fallback tetap 30 menit kalau env var gak ada) — HANYA buat automated testing, jangan di-set di `.env` production. `tests/e2e/run-with-short-timeout.js` (dipanggil via `npm run test:e2e`) otomatis set ke 10 detik sebelum jalanin Playwright dan **selalu balikin `.env` ke isi semula di `finally`**, sukses maupun gagal.
  - **Playwright di-setup dari nol** (`@playwright/test` + Chromium) — instalasi sempat 2x gagal karena `registry.npmjs.org` gak bisa diakses (jaringan flap lagi, beda host dari `github.com`/`packagist.org` yang udah pulih), berhasil lewat mirror `--registry https://registry.npmmirror.com`. 1 test e2e (`tests/e2e/session-timeout.spec.js`) nutup 2 fitur JS-only yang gak bisa dites PHPUnit: warning idle timeout + draft auto-save & restore. **Catatan teknis buat siapa pun yang nulis Playwright test lain di app ini**: (a) `baseURL` WAJIB trailing slash + path navigasi JANGAN pakai leading slash (`page.goto('chimei/add')` bukan `'/chimei/add'`) — `baseURL` punya subpath (`/form-am`), leading slash bikin URL resolve balik ke root domain, ngilangin subpath-nya; (b) `WARNING_MS` di modal timeout itu **hardcode 5 menit**, gak proporsional ke `SESSION_TIMEOUT_SECONDS` — kalau di-override pendek (10 detik), modal peringatan bakal nongol PRAKTIS INSTAN dan TERUS reopen tiap di-reset, jadi interaksi form pas modal nutupin layar HARUS lewat `page.evaluate()` (manipulasi DOM + dispatch event langsung), bukan klik/fill normal yang bakal ke-block sama overlay full-screen.
  - Semua 38 test PHPUnit + 1 test Playwright **lolos** di akhir sesi, DB dicek manual balik bersih (termasuk 2 akun throwaway sisa dari iterasi debugging lockout sebelum bug search-nya kefix, ketauan & dibersihin).
  - Dokumentasi: `DOCS_MD/AUTOMATED_TESTING.md` di-update, `package.json`/`playwright.config.js`/`tests/e2e/` baru.

- **Round 38 (bebersih dokumentasi total buat handover, atas permintaan user "kalau udah clean & ready di-handover ke orang server").** User minta: bikin `README.md` yang jelasin semuanya, terus hapus dokumentasi `DOCS_MD/` yang udah gak relevan, sisain cuma 5 kategori: URS, kesesuaian program vs URS, dokumentasi teknis, evaluasi program, panduan bikin PPT. Dikonfirmasi dulu 2 hal ke user sebelum eksekusi (opsi "pecah beberapa file teknis" vs "1 file besar", dan nasib `REPORT.md`/`dummy_user.md`) — user pilih opsi recommended di keduanya.
  - **File baru**: [README.md](./README.md) (root, entry point handover), `DOCS_MD/KEPATUHAN_URS.md` (merge `GAP_URS.md` + `CHECKLIST_KEPATUHAN_URS.md`, update ke temuan Round 34-37: XSS fix, 3 bug Round 37, jumlah modul dikoreksi ke 17 dari angka lama "19"), `DOCS_MD/TECHNICAL_OVERVIEW.md` (arsitektur `BaseMachineController`, pola tabel per-mesin, cara nambah mesin baru — versi update dari `PROMPT_MESIN_BARU.md` yang polanya udah berubah total sejak Round 33, plus known-gaps & skip-list dari `FINAL_IMPROVEMENT.md`), `DOCS_MD/DEPLOYMENT.md` (rename dari `DEPLOYMENT_PLAN.md`, isi sama + rujukan ke tools testing baru), `DOCS_MD/TESTING.md` (merge `AUTOMATED_TESTING.md` + `MANUAL_TESTING_CHECKLIST.md` + `dummy_user.md`, item checklist manual yang udah keng-cover otomatis ditandain ⚙️), `DOCS_MD/PANDUAN_PRESENTASI.md` (baru, outline 11 slide + talking points buat bikin PPT).
  - **`EVALUATION.md` dapet section baru** "Ringkasan Eksekutif (Non-Teknis)" di paling atas (ditarik dari isi `REPORT.md` yang sekarang dihapus, angka mesin dikoreksi ke 17) — detail teknis kronologis per-round di bawahnya TETAP UTUH, gak diringkas/dipotong (44KB → 47KB, cuma nambah bukan ngurangin).
  - **12 file dihapus**: `ANALYSIS.md`, `Improvement.md` (udah lama ditandain usang/arsip), `PROMPT_MESIN_BARU.md` (isinya udah diserap ke `TECHNICAL_OVERVIEW.md`, versi lama udah gak match pola `BaseMachineController`), `GAP_URS.md` + `CHECKLIST_KEPATUHAN_URS.md` (di-merge), `DEPLOYMENT_PLAN.md` (di-rename), `AUTOMATED_TESTING.md` + `MANUAL_TESTING_CHECKLIST.md` + `dummy_user.md` (di-merge), `REPORT.md` (di-merge ke `EVALUATION.md`), `FINAL_IMPROVEMENT.md` (isi relevan diserap ke `TECHNICAL_OVERVIEW.md`), `readme.txt` (isinya cuma "test234", diganti `README.md`).
  - Dicek dulu (grep seluruh project) sebelum hapus: gak ada broken markdown link ke file-file yang dihapus — mention nama file lama yang masih ada di histori `CLAUDE.md`/`EVALUATION.md` itu narasi historis (menjelaskan apa yang terjadi waktu itu), bukan link navigasi aktif, jadi sengaja dibiarkan apa adanya.
  - Struktur final `DOCS_MD/`: `URS_Form Autonomous Maintenance (1).md/.pdf` + lampiran `.docx` (sumber resmi), `KEPATUHAN_URS.md`, `TECHNICAL_OVERVIEW.md`, `DEPLOYMENT.md`, `TESTING.md`, `EVALUATION.md`, `PANDUAN_PRESENTASI.md` — persis 5 kategori yang diminta user + dokumen sumber URS.

- **Round 39 (audit ulang "beneran ready buat handover?" + tambah tutorial test, atas permintaan user).** User minta cek ulang jujur apa masih ada yang perlu diperbaiki, plus tambahin `tests/README.md` (tutorial jalanin test) dan `requirements.txt`.
  - **Bug nyata #1 ditemukan**: `.gitignore` punya rule blanket `*.sql` yang gak sengaja nge-gitignore **seluruh `database/migrations/` (18 file) + `database/postgres/01_schema.sql`+`02_seed.sql`** — 0 file ke-track di git. Kalau di-handover lewat git clone, folder `database/` bakal kosong sama sekali, gak ada cara rebuild skema dari nol. Rule ini awalnya buat exclude dump backup gede (`ddl_dml.sql` dkk, udah dihapus Round 28) tapi terlalu luas cakupannya. **Fixed**: rule `*.sql` dihapus dari `.gitignore` (dicek dulu gak ada file `.sql` lain di luar `database/` yang perlu di-exclude), sekalian nambahin `.phpunit.result.cache` + `error.log` yang sebelumnya gak ke-ignore (harusnya emang di-ignore, generated artifact).
  - **Bug nyata #2 ditemukan**: `Dockerfile` cuma install extension PHP `mysqli`/`pdo_mysql` — padahal stack sekarang Postgres (`pdo_pgsql`/`pgsql`) dan Excel export butuh `zip` (ketemu bug-nya Round 37). Kalau dipakai apa adanya, container bakal gagal connect DB total dan Excel export gak akan pernah jalan. **Fixed**: ditambahin `pdo_pgsql pgsql zip` + system library yang dibutuhin (`libpq-dev libzip-dev` via `apt-get`).
  - **Verifikasi fix Dockerfile**: dicoba `docker build` beneran (Docker Desktop sempet mati, dinyalain via command line `Start-Process`/background — TERNYATA proses background yang di-start dari 1 tool call bisa ke-reap kalau tool call itu selesai, ini juga yang bikin Apache sempet mati berkali-kali sepanjang sesi-sesi sebelumnya; workaround yang stabil: `nohup ./httpd.exe & disown` dari Bash, bukan PowerShell `Start-Process`). Build **gagal** — tapi diisolasi dulu sebelum nyerah: dicoba `apt-get update` di image `php:8.3-apache` POLOS TANPA modifikasi apapun, **ternyata gagal juga** (403 Forbidden dari `deb.debian.org` lewat Fastly CDN) — jadi kebukti ini masalah environment/jaringan (mirror Debian nolak request dari jaringan ini saat ini), bukan kesalahan di perubahan Dockerfile. Root-cause extension yang ditambahin (nama & system library-nya) tetap correct secara teknis (pola standar `docker-php-ext-install`), cuma belum bisa dibuktikan build selesai end-to-end di sesi ini. Dicatat di `tests/requirements.txt` sebagai known issue.
  - **File baru**: `tests/README.md` (tutorial singkat jalanin test — prasyarat, install, run, baca hasil, cleanup) dan `tests/requirements.txt` (daftar versi software yang dibutuhin: PHP 8.2+, PostgreSQL 17, Composer, Node 18+/npm, plus catatan Docker di atas). `DOCS_MD/TESTING.md` ditambahin link ke keduanya di paling atas.
  - **Full regression check**: `vendor/bin/phpunit --testdox` tetap 38/38 hijau, Apache/app masih normal — perubahan sesi ini murni file config (`.gitignore`, `Dockerfile`) + dokumentasi baru, gak nyentuh kode aplikasi sama sekali.
  - **Kesimpulan buat user**: aplikasi (kode + testing) **beneran ready** — 2 gap yang ketemu itu infra/tooling (git tracking, Docker), bukan bug aplikasi. Yang masih PENDING BUKAN kesalahan, tapi keputusan yang emang harus dari manusia: deployment production (`DEPLOYMENT.md`, keputusan role mapping & jadwal downtime) dan konten part Illapak 1-12/Unifill B yang masih placeholder.

- **Round 40 (bikin `tests/TEST_CASES.md` atas permintaan user, OPcache dienable, 1 bug UI nyata ketemu & difix).**
  - **`tests/TEST_CASES.md` dibikin** — 40 test case dikelompokkan per kategori (Auth/Lockout/RBAC/CRUD/Approval/Export/Audit Trail/XSS/Smoke 17-modul/E2E), semua dijalanin ulang & statusnya dicek real-time sebelum ditulis (bukan nyalin angka lama). Di-link dari `tests/README.md`.
  - **OPcache PHP diaktifkan** (`zend_extension=opcache` + `opcache.enable=1` di `php.ini` XAMPP, sebelumnya mati/default) — user nanya soal robustness buat beban tinggi, disepakati skala pabrik (17 mesin × 3 shift ≈ puluhan-ratusan operator, bukan ribuan) gak butuh infra tambahan (load balancer/caching layer), tapi OPcache itu zero-cost/zero-downside jadi tetap di-enable. Diverifikasi: `php -m` confirm loaded, Apache di-restart (pola `nohup ./httpd.exe & disown`, bukan PowerShell `Start-Process` yang kebukti gampang ke-reap), full test suite tetap 38/38 hijau. Dicatat di `TECHNICAL_OVERVIEW.md` + checklist `DEPLOYMENT.md` (WAJIB diaktifin juga pas setup server production, ini setting `php.ini` server, gak ke-bawa otomatis dari kode).
  - **Bug UI nyata ditemukan user via screenshot**: halaman `edit_data` (operator mengoreksi data submission-nya sendiri) **gak nampilin gambar part sama sekali** di SEMUA 17 modul (termasuk SIG) — gambar cuma ada di `add.php`, padahal operator paling butuh liat foto acuan justru pas lagi ngoreksi. Dicek: bukan regresi/kehapus, emang dari awal `edit_data.php` gak pernah punya markup gambar (beda desain dari `add.php` sejak dibikin). **Fixed**: script PHP `str_replace` literal (verifikasi count sebelum replace tiap anchor, 3 anchor per file × 17 file) nyuntik `$image_names` array (diekstrak dari `add.php` masing-masing modul) + kolom gambar (pola sama persis kayak `add.php`: `<a class="part-image-link">` + fallback "Gambar belum diunggah") ke `edit_data.php` semua 17 modul, termasuk SIG (kasus antistatic 3-opsi dicek khusus, tetap aman).
  - **Diverifikasi live beneran** (bukan cuma lint): submit Chimei + SIG live, buka `edit_data`, cek `<img src=".../chimei conveyor produk.png">` dkk bener-bener muncul di HTML response (bukan cuma elemen kosong), data test dihapus lagi. **Regresi guard ditambahin ke `MachineCrudTest.php`** (assert `part-image-link` ada di response `edit_data` buat Chimei/Illapak 1-2/SIG) biar gak kejadian lagi diam-diam ke depan. Full suite: 38/38 tetap hijau, 401 assertion (naik dari 397).

- **Round 41 (koreksi format validasi NIK, dikonfirmasi langsung ke user).** Diskusi soal kepatuhan URS 1.1 ("username menggunakan NIK") — ternyata URS **gak nyebut format/panjang spesifik**, cuma "wajib NIK" doang (beda sama password yang eksplisit "8 karakter"). Implementasi lama (`^[A-Za-z0-9]{8}$`, huruf+angka campur) itu asumsi tim development, bukan kutipan URS. Dikonfirmasi ke user: NIK di sini = **Nomor Induk Karyawan pabrik** (bukan NIK KTP 16 digit), **full angka, tepat 8 digit**.
  - **Fixed**: `is_valid_nik_username()` di `helpers/Functions.php` → regex `^[0-9]{8}$`. Pesan error di `IndexController::register()` & `UsersController::add()` diupdate. Hint/placeholder UI di `register.php` & `users/add.php` diupdate (+ atribut `pattern="[0-9]{8}"`).
  - **Akun dummy testing (`MANAGE01`/`SUPERV01`/`STAFOP01`) TETAP AMAN** — gak perlu exception khusus, karena validasi ini cuma jalan pas `add()`/registrasi akun BARU, gak pernah di-recheck ulang pas login atau edit akun yang udah ada. Akun lama (termasuk `superadmin`) gak kena dampak apapun.
  - **Test diperluas** atas permintaan user ("test registrasi per role belum masuk kan?") — `RegistrationTest.php` sebelumnya cuma 1 test hardcode role 4, sekarang jadi data-provider **4 role sekaligus** (Administrator/Manager/Supervisor/Staff-Operator — form registrasi publik emang ngizinin milih role manapun, gak dibatasi, aktivasi manual admin yang jadi gate-nya) + 1 test negatif baru (username format lama huruf+angka sekarang harus DITOLAK). Generator username di `RegistrationTest.php` & `LockoutTest.php` ikut diubah dari alfanumerik ke 8-digit-angka biar konsisten sama rule baru.
  - **Diverifikasi live end-to-end**: `vendor/bin/phpunit tests/Feature/RegistrationTest.php tests/Feature/LockoutTest.php` — 6/6 lolos (5 registrasi termasuk 4 role + 1 negatif, 1 lockout). Full suite tetap hijau.
  - Dicatat di `KEPATUHAN_URS.md` sebagai koreksi (bukan gap baru — item URS 1.1 tetap ✅, cuma detail implementasinya diluruskan).

Langkah selanjutnya (semua blocker & gap fungsionalitas Filling **sudah beres** per Round 13 — sisanya kerjaan konten/data, bukan kode):
1. User mengisi/mengganti nama part tiap mesin Illapak 1-12 dan Unifill B sesuai spesifikasi mesin sebenarnya (ubah `$parts` array di controller + kolom di SQL migration + re-create tabel) — saat ini masih placeholder/generic.
2. User mengunggah gambar part ke `assets/images/{nama_mesin}/` untuk tiap mesin.
3. User mengisi Metode/Alat/Standard/Durasi/Pelaksanaan yang masih placeholder.
4. Lakukan submit AM manual (OK dan NOK) untuk validasi end-to-end per mesin dengan data part yang sudah final.
5. Mulai Compounding dan/atau Packaging kalau daftar mesinnya sudah siap.

Item lain yang masih pending (prioritas lebih rendah dari di atas):
- `#8` `TelegramNotifier` helper — lama, gak urgent dibanding bikin mesin baru.
- Keputusan skema `kendala_detail` (1 tabel generik vs 1 tabel per mesin) buat `BaseMachineController` — makin relevan karena semua 15 mesin Filling sudah ada, bisa di-refactor sekaligus.

**State lingkungan lokal (per Round 27-28):** Database lokal sekarang **PostgreSQL 17** (standalone install, port 5432, db `form_am_plg`, user `formam`), **BUKAN MySQL lagi** — MySQL/XAMPP udah dihapus total dari mesin ini. Stack: Apache (XAMPP) + PHP (XAMPP, ekstensi `pdo_pgsql`/`pgsql` udah diaktifkan di `php.ini`) + Postgres (terpisah dari XAMPP). Kredensial di `.env` (gitignored). Kalau sesi depan connection DB gagal: cek service `postgresql-x64-17` nyala (`Get-Service postgresql-x64-17`), bukan cek MySQL. Apache XAMPP biasanya perlu dinyalain manual tiap sesi baru (`/c/xampp/apache/bin/httpd.exe`).

## Cara Kerja yang Disukai User (dari sesi-sesi sebelumnya)

- Refactor besar (nyentuh banyak file/method) lebih baik pakai script transformasi otomatis + verifikasi programatis, bukan copy-paste manual — lihat pendekatan Round 3 di `EVALUATION.md` sebagai contoh.
- Item yang butuh testing manual berat (race condition, refactor 56 controller) jangan buru-buru dikerjain dalam sesi singkat — tanya dulu / tunda ke sesi dengan waktu lebih.
- Kalau ada pilihan trade-off (effort vs risiko), tawarkan opsi dulu ke user sebelum eksekusi.
- **Setelah scan awal buat ngukur scope suatu bug (grep pattern tertentu), jangan langsung percaya angkanya** — di Round 5 pattern regex pertama cuma nangkep "8 file" padahal aslinya 33, gara-gara variasi nama kolom yang gak ke-cover. Selalu **re-scan pakai pattern lebih longgar setelah patch selesai** buat mastiin gak ada yang kelewat, sebelum lapor "selesai" ke user.
